# Java + Spring Boot + MyBatis 项目知识体系闭环设计

> **目标场景**: 从存量代码中提取知识 → 减少重复踩坑 → 提升代码一致性 → 加速功能开发
> **技术栈**: Java + Spring Boot + MyBatis

---

## 一、知识闭环总览图

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                              知 识 体 系 闭 环                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                 │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   提取层     │    │   存储层     │    │   检索层     │    │   应用层     │  │
│  │  EXTRACT     │───▶│   STORE      │───▶│   RETRIEVE   │───▶│   APPLY      │  │
│  └──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘  │
│         │                   │                   │                   │          │
│         │                   │                   │                   │          │
│         ▼                   ▼                   ▼                   ▼          │
│  ┌──────────────────────────────────────────────────────────────────────────┐  │
│  │                           反 馈 层  FEEDBACK                              │  │
│  │                                                                          │  │
│  │   问题解决 ──▶ /workflows:compound ──▶ docs/solutions/ ──▶ 下次复用     │  │
│  └──────────────────────────────────────────────────────────────────────────┘  │
│                                                                                 │
└─────────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、四层架构详解

### 第 1 层：知识提取层 (EXTRACT)

从存量 Java 代码中提取三类知识：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         知识提取层 EXTRACT                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   📂 存量代码                                                                │
│   ════════════                                                              │
│                                                                             │
│   ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐            │
│   │  结构性知识      │  │  演化性知识      │  │  模式性知识      │            │
│   │  (WHAT)         │  │  (WHY)          │  │  (HOW)          │            │
│   └────────┬────────┘  └────────┬────────┘  └────────┬────────┘            │
│            │                    │                    │                      │
│            ▼                    ▼                    ▼                      │
│   ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐            │
│   │ repo-research-  │  │ git-history-    │  │ pattern-        │            │
│   │ analyst         │  │ analyzer        │  │ recognition-    │            │
│   │                 │  │                 │  │ specialist      │            │
│   └─────────────────┘  └─────────────────┘  └─────────────────┘            │
│                                                                             │
│   提取内容:                                                                  │
│   • 项目结构约定        • 代码演变历史        • 设计模式使用                    │
│   • 分层架构规范        • 重构决策背景        • 命名约定                        │
│   • 配置文件模式        • 贡献者专业领域      • 反模式警告                       │
│   • Spring Bean 组织   • 关键变更点          • 代码重复检测                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 提取的知识类型映射（Java 项目）

| 知识类型 | 提取 Agent | 提取来源 | 输出内容 |
|---------|-----------|---------|---------|
| **结构性知识** | `repo-research-analyst` | 项目目录、pom.xml、application.yml、README | 项目结构规范、模块划分、配置约定 |
| **演化性知识** | `git-history-analyzer` | git log、git blame、commit messages | 重构原因、设计决策背景、问题修复历史 |
| **模式性知识** | `pattern-recognition-specialist` | Java 源码、Spring 注解、MyBatis XML | 设计模式、命名约定、分层规范、反模式 |

#### Java/Spring Boot 特定提取点

```
src/main/java/
├── controller/          ← 提取: REST API 设计模式、参数校验模式
├── service/             ← 提取: 业务逻辑组织、事务管理模式
├── repository/          ← 提取: MyBatis Mapper 约定、SQL 模式
├── model/entity/        ← 提取: 实体设计、关联映射模式
├── dto/                 ← 提取: DTO 转换模式
├── config/              ← 提取: Spring 配置约定
└── common/exception/    ← 提取: 异常处理模式

src/main/resources/
├── mapper/              ← 提取: MyBatis XML 编写规范、动态 SQL 模式
├── application*.yml     ← 提取: 配置分层、Profile 使用约定
└── db/migration/        ← 提取: 数据库变更管理模式
```

---

### 第 2 层：知识存储层 (STORE)

将提取的知识结构化存储：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         知识存储层 STORE                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   docs/                                                                     │
│   ├── patterns/                    ← 🎯 代码模式（目标 C: 一致性）            │
│   │   ├── critical-patterns.md         必读模式清单                          │
│   │   ├── controller-patterns.md       Controller 层规范                     │
│   │   ├── service-patterns.md          Service 层规范                        │
│   │   ├── mybatis-patterns.md          MyBatis 使用规范                      │
│   │   ├── exception-patterns.md        异常处理规范                          │
│   │   └── naming-conventions.md        命名约定                              │
│   │                                                                         │
│   ├── solutions/                   ← 🎯 问题解决方案（目标 B: 减少踩坑）        │
│   │   ├── performance-issues/          性能问题                              │
│   │   │   ├── n-plus-one-mybatis.md                                         │
│   │   │   └── connection-pool-exhausted.md                                  │
│   │   ├── database-issues/             数据库问题                            │
│   │   │   ├── transaction-rollback.md                                       │
│   │   │   └── deadlock-resolution.md                                        │
│   │   ├── integration-issues/          集成问题                              │
│   │   │   └── redis-timeout.md                                              │
│   │   └── runtime-errors/              运行时错误                            │
│   │       └── null-pointer-patterns.md                                      │
│   │                                                                         │
│   ├── brainstorms/                 ← 🎯 设计探索（目标 D: 加速开发）           │
│   │   └── YYYY-MM-DD-<topic>-brainstorm.md                                  │
│   │                                                                         │
│   └── plans/                       ← 🎯 实现计划（目标 D: 加速开发）           │
│       └── YYYY-MM-DD-<type>-<name>-plan.md                                  │
│                                                                             │
│   CLAUDE.md                        ← 🎯 项目级指令（AI 代理必读）              │
│   └── 包含: 项目约定、技术栈、关键模式引用                                      │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 存储结构与目标映射

| 目标 | 存储位置 | 存储内容 | 触发更新时机 |
|-----|---------|---------|------------|
| **B: 减少踩坑** | `docs/solutions/**/*.md` | 问题症状、根因分析、解决方案、预防措施 | 问题解决后 (`/workflows:compound`) |
| **C: 代码一致性** | `docs/patterns/*.md` | 代码模式、命名约定、分层规范、反模式警告 | 初始提取 + 发现新模式时 |
| **D: 加速开发** | `docs/brainstorms/` + `docs/plans/` | 设计决策、实现方案、参考代码 | 功能规划时 |

#### CLAUDE.md 模板（Java 项目）

```markdown
# [项目名称] - AI 代理指令

## 技术栈
- Java 17 + Spring Boot 3.x
- MyBatis Plus / MyBatis
- MySQL / PostgreSQL
- Redis

## 项目结构约定
[由 repo-research-analyst 提取并填充]

## 必读模式 (Critical Patterns)
- 📖 [Controller 层规范](docs/patterns/controller-patterns.md)
- 📖 [Service 层规范](docs/patterns/service-patterns.md)
- 📖 [MyBatis 使用规范](docs/patterns/mybatis-patterns.md)
- 📖 [异常处理规范](docs/patterns/exception-patterns.md)

## 常见问题速查
- 🔧 [N+1 查询问题](docs/solutions/performance-issues/n-plus-one-mybatis.md)
- 🔧 [事务回滚问题](docs/solutions/database-issues/transaction-rollback.md)

## 命名约定
[由 pattern-recognition-specialist 提取并填充]

## 禁止事项
- ❌ 在 Controller 中直接操作数据库
- ❌ 在 Service 方法中捕获并吞掉异常
- ❌ 使用 SELECT * 查询
- ❌ [从反模式检测中提取]
```

---

### 第 3 层：知识检索层 (RETRIEVE)

在工作流各阶段自动检索相关知识：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         知识检索层 RETRIEVE                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   工作流阶段              检索 Agent                检索内容                  │
│   ══════════════════════════════════════════════════════════════════════    │
│                                                                             │
│   /workflows:brainstorm                                                     │
│        │                                                                    │
│        ├──▶ repo-research-analyst  ──▶  现有类似功能、项目约定               │
│        │                                                                    │
│        └──▶ 输出: docs/brainstorms/YYYY-MM-DD-<topic>-brainstorm.md        │
│                                                                             │
│   /workflows:plan                                                           │
│        │                                                                    │
│        ├──▶ repo-research-analyst  ──▶  代码模式、CLAUDE.md 指令            │
│        │                                                                    │
│        ├──▶ learnings-researcher   ──▶  docs/solutions/ 已有解决方案         │
│        │         │                                                          │
│        │         └── 高效检索策略:                                           │
│        │             1. Grep 预过滤 (tags, module, symptoms)                │
│        │             2. 仅读取候选文件 frontmatter                           │
│        │             3. 完整读取强匹配文件                                    │
│        │                                                                    │
│        ├──▶ best-practices-researcher ──▶ Spring Boot 最佳实践 (条件性)     │
│        │                                                                    │
│        ├──▶ spec-flow-analyzer     ──▶  用户流程分析、边缘案例               │
│        │                                                                    │
│        └──▶ 输出: docs/plans/YYYY-MM-DD-<type>-<name>-plan.md              │
│                                                                             │
│   /workflows:work                                                           │
│        │                                                                    │
│        ├──▶ 读取计划文件中引用的代码模式                                      │
│        │                                                                    │
│        ├──▶ 遵循 docs/patterns/*.md 中的规范                                │
│        │                                                                    │
│        └──▶ 输出: 代码变更 + PR                                             │
│                                                                             │
│   /workflows:review                                                         │
│        │                                                                    │
│        ├──▶ pattern-recognition-specialist ──▶ 检测违反模式的代码            │
│        │                                                                    │
│        ├──▶ architecture-strategist ──▶ 架构一致性检查                      │
│        │                                                                    │
│        └──▶ 输出: todos/*.md 审查发现                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 检索时机与策略

| 工作流阶段 | 检索时机 | 检索策略 | 检索目的 |
|-----------|---------|---------|---------|
| **brainstorm** | 开始探索时 | 轻量级仓库扫描 | 理解现有模式，避免重新发明 |
| **plan** | 研究阶段 | 并行检索（本地+条件性外部） | 收集相关知识，制定informed计划 |
| **work** | 实现前 | 读取计划引用的模式文件 | 确保代码一致性 |
| **review** | PR 审查时 | 模式匹配检测 | 发现违反约定的代码 |
| **compound** | 问题解决后 | 搜索相似问题 | 关联已有知识，避免重复记录 |

---

### 第 4 层：知识应用层 (APPLY)

将检索到的知识应用到实际工作中：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         知识应用层 APPLY                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   目标 B: 减少重复踩坑                                                        │
│   ════════════════════                                                      │
│                                                                             │
│   实现前检查                         实现中参考                               │
│   ┌─────────────────┐               ┌─────────────────┐                     │
│   │ learnings-      │               │ 查阅匹配的      │                     │
│   │ researcher      │──────────────▶│ docs/solutions/ │                     │
│   │ 搜索相似问题     │               │ 文档           │                     │
│   └─────────────────┘               └─────────────────┘                     │
│           │                                  │                              │
│           ▼                                  ▼                              │
│   "发现 3 个相关的已解决问题:         "根据 n-plus-one-mybatis.md:           │
│    - n-plus-one-mybatis.md            使用 @ResultMap + 嵌套查询            │
│    - connection-pool.md               而不是循环查询"                        │
│    - transaction-rollback.md"                                               │
│                                                                             │
│   ─────────────────────────────────────────────────────────────────────     │
│                                                                             │
│   目标 C: 提升代码一致性                                                      │
│   ══════════════════════                                                    │
│                                                                             │
│   代码生成前                          代码审查时                              │
│   ┌─────────────────┐               ┌─────────────────┐                     │
│   │ 读取            │               │ pattern-        │                     │
│   │ docs/patterns/  │──────────────▶│ recognition-    │                     │
│   │ *.md            │               │ specialist      │                     │
│   └─────────────────┘               └─────────────────┘                     │
│           │                                  │                              │
│           ▼                                  ▼                              │
│   "根据 service-patterns.md:          "检测到违反模式:                       │
│    - Service 方法使用 @Transactional   - Controller 中直接调用 Mapper        │
│    - 返回 DTO 而非 Entity              - 缺少 @Validated 注解                │
│    - 异常使用自定义 BusinessException"  建议: 参考 controller-patterns.md"   │
│                                                                             │
│   ─────────────────────────────────────────────────────────────────────     │
│                                                                             │
│   目标 D: 加速功能开发                                                        │
│   ════════════════════                                                      │
│                                                                             │
│   规划阶段                           实现阶段                                 │
│   ┌─────────────────┐               ┌─────────────────┐                     │
│   │ 检索            │               │ 读取计划中      │                     │
│   │ docs/brainstorms│──────────────▶│ 引用的参考代码  │                     │
│   │ (14天内相关)     │               │ 和设计决策      │                     │
│   └─────────────────┘               └─────────────────┘                     │
│           │                                  │                              │
│           ▼                                  ▼                              │
│   "找到相关设计:                       "参考 UserService:42 的实现模式        │
│    user-auth-brainstorm.md            快速实现 OrderService"                │
│    跳过细化问题，直接使用设计决策"                                             │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

### 第 5 层：反馈层 (FEEDBACK) - 闭环核心

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         反馈层 FEEDBACK (闭环核心)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                                                                     │   │
│   │    问题解决                                                          │   │
│   │        │                                                            │   │
│   │        ▼                                                            │   │
│   │   /workflows:compound                                               │   │
│   │        │                                                            │   │
│   │        ├──▶ 提取症状、根因、解决方案                                  │   │
│   │        │                                                            │   │
│   │        ├──▶ 分类 (problem_type)                                     │   │
│   │        │    • performance_issue → docs/solutions/performance-issues/│   │
│   │        │    • database_issue → docs/solutions/database-issues/      │   │
│   │        │    • runtime_error → docs/solutions/runtime-errors/        │   │
│   │        │                                                            │   │
│   │        ├──▶ 创建解决方案文档                                          │   │
│   │        │                                                            │   │
│   │        └──▶ 决策菜单:                                                │   │
│   │             ├─ 1. Continue workflow                                 │   │
│   │             ├─ 2. Add to Required Reading ← 提升为必读模式 ⭐         │   │
│   │             ├─ 3. Link related issues                               │   │
│   │             ├─ 4. Update CLAUDE.md ← 更新项目指令                    │   │
│   │             └─ 5. Create pattern doc ← 新增模式文档                  │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                         知识提升路径                                  │   │
│   │                                                                     │   │
│   │   单次解决方案                                                        │   │
│   │   docs/solutions/xxx.md                                             │   │
│   │        │                                                            │   │
│   │        │ (同类问题出现 3+ 次)                                         │   │
│   │        ▼                                                            │   │
│   │   通用模式                                                           │   │
│   │   docs/patterns/xxx-patterns.md                                     │   │
│   │        │                                                            │   │
│   │        │ (高频/高风险模式)                                            │   │
│   │        ▼                                                            │   │
│   │   必读清单                                                           │   │
│   │   docs/patterns/critical-patterns.md                                │   │
│   │        │                                                            │   │
│   │        │ (项目级强制约定)                                             │   │
│   │        ▼                                                            │   │
│   │   CLAUDE.md (禁止事项)                                               │   │
│   │                                                                     │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、完整闭环流程图

```
                              ┌─────────────────────┐
                              │    存量 Java 代码    │
                              │   (Spring Boot +    │
                              │    MyBatis)         │
                              └──────────┬──────────┘
                                         │
                    ┌────────────────────┼────────────────────┐
                    │                    │                    │
                    ▼                    ▼                    ▼
           ┌───────────────┐    ┌───────────────┐    ┌───────────────┐
           │ repo-research │    │ git-history   │    │ pattern-      │
           │ -analyst      │    │ -analyzer     │    │ recognition   │
           │               │    │               │    │ -specialist   │
           │ 结构性知识     │    │ 演化性知识     │    │ 模式性知识     │
           └───────┬───────┘    └───────┬───────┘    └───────┬───────┘
                   │                    │                    │
                   └────────────────────┼────────────────────┘
                                        │
                                        ▼
              ┌─────────────────────────────────────────────────────┐
              │                    知识存储                          │
              │                                                     │
              │  CLAUDE.md ◄──── docs/patterns/ ◄──── docs/solutions│
              │  (项目指令)       (代码模式)          (解决方案)       │
              │                                                     │
              └───────────────────────┬─────────────────────────────┘
                                      │
          ┌───────────────────────────┼───────────────────────────┐
          │                           │                           │
          ▼                           ▼                           ▼
┌─────────────────┐        ┌─────────────────┐        ┌─────────────────┐
│ /workflows:     │        │ /workflows:     │        │ /workflows:     │
│ brainstorm      │───────▶│ plan            │───────▶│ work            │
│                 │        │                 │        │                 │
│ 检索: 现有模式   │        │ 检索: 解决方案   │        │ 应用: 代码模式   │
│       约定      │        │       最佳实践   │        │       生成代码   │
└─────────────────┘        └─────────────────┘        └────────┬────────┘
                                                               │
                                                               ▼
                                                     ┌─────────────────┐
                                                     │ /workflows:     │
                                                     │ review          │
                                                     │                 │
                                                     │ 检测: 模式违反   │
                                                     │       一致性    │
                                                     └────────┬────────┘
                                                              │
                    ┌─────────────────────────────────────────┘
                    │
                    ▼
          ┌─────────────────┐
          │   问题解决       │
          │   (调试/修复)    │
          └────────┬────────┘
                   │
                   ▼
          ┌─────────────────┐        ┌─────────────────────────┐
          │ /workflows:     │        │                         │
          │ compound        │───────▶│  docs/solutions/xxx.md  │
          │                 │        │                         │
          │ 文档化解决方案   │        │  ↓ (出现 3+ 次)          │
          └─────────────────┘        │                         │
                   │                 │  docs/patterns/xxx.md   │
                   │                 │                         │
                   │                 │  ↓ (高频/高风险)         │
                   │                 │                         │
                   │                 │  critical-patterns.md   │
                   │                 │                         │
                   │                 │  ↓ (项目强制)            │
                   │                 │                         │
                   │                 │  CLAUDE.md              │
                   │                 │                         │
                   │                 └───────────┬─────────────┘
                   │                             │
                   └─────────────────────────────┘
                            闭  环  完  成
                   (下次 plan/work 时自动检索复用)
```

---

## 四、Java 项目特定的 Agent 配置建议

### 需要调整的 Agents

由于 compound-engineering 原本是为 Rails 设计的，以下 agents 需要适配 Java 生态：

| 原 Agent | Java 适配 | 调整内容 |
|---------|----------|---------|
| `kieran-rails-reviewer` | → `java-spring-reviewer` | 审查 Spring Boot 约定、Bean 管理、事务 |
| `dhh-rails-reviewer` | → 可选移除或替换 | 换成 Spring 官方风格指南 |
| `rails-turbo-expert` | → 移除 | 不适用于后端 Java 项目 |
| `data-migration-expert` | → `flyway-migration-expert` | 适配 Flyway/Liquibase |

### 建议新增的 Agents

| Agent 名称 | 职责 | 审查内容 |
|-----------|------|---------|
| `mybatis-reviewer` | MyBatis 规范审查 | SQL 注入风险、N+1 查询、ResultMap 使用 |
| `spring-transaction-reviewer` | 事务审查 | @Transactional 使用、传播行为、回滚规则 |
| `java-concurrency-reviewer` | 并发审查 | 线程安全、锁使用、CompletableFuture |

### 模式检测适配

`pattern-recognition-specialist` 需要识别的 Java 特定模式：

```yaml
# 设计模式检测
positive_patterns:
  - "Builder 模式 (@Builder)"
  - "Factory 模式 (xxxFactory)"
  - "Strategy 模式 (xxxStrategy interface)"
  - "Repository 模式 (@Repository)"

# 反模式检测
anti_patterns:
  - "Controller 中直接 @Autowired Mapper"
  - "Service 方法缺少 @Transactional"
  - "Entity 直接暴露给 Controller"
  - "SELECT * 查询"
  - "循环内数据库查询 (N+1)"
  - "catch (Exception e) 空处理"
```

---

## 五、初始化流程（从存量代码启动知识体系）

```bash
# Step 1: 创建知识目录结构
mkdir -p docs/{patterns,solutions/{performance-issues,database-issues,runtime-errors,integration-issues},brainstorms,plans}

# Step 2: 运行结构分析（一次性）
claude agent repo-research-analyst "分析项目结构、Spring Boot 配置约定、模块划分"
# → 输出整理到 CLAUDE.md

# Step 3: 运行模式提取（一次性）
claude agent pattern-recognition-specialist "识别代码中的设计模式、命名约定、分层规范"
# → 输出整理到 docs/patterns/*.md

# Step 4: 分析关键模块历史（可选）
claude agent git-history-analyzer "分析 service 层的演变历史和重大重构"
# → 关键决策记录到 docs/patterns/ 或 CLAUDE.md

# Step 5: 创建 critical-patterns.md（从现有代码中提炼）
# 手动或用 AI 整理高频/高风险模式

# Step 6: 更新 CLAUDE.md
# 添加必读模式引用、禁止事项、项目约定

# 闭环已启动！
# 后续通过 /workflows:compound 持续积累解决方案
```

---

## 六、知识复合效益预估

| 阶段 | 第 1 次 | 第 2 次 | 第 N 次 |
|-----|--------|--------|--------|
| **遇到 N+1 查询问题** | 调试 30 分钟 | 查文档 5 分钟 | AI 自动避免 |
| **实现新 Service** | 从零设计 | 参考模式文档 | AI 遵循模式生成 |
| **理解事务边界** | 查源码 + 文档 | 读 patterns | AI 提前检查 |

**知识复合公式**：

```
第 N 次解决同类问题的时间 = 第 1 次时间 × (1 / log(N+1))

假设第 1 次解决 N+1 问题花费 30 分钟：
- 第 2 次: 30 × (1/log3) ≈ 30 × 0.63 ≈ 19 分钟
- 第 3 次: 30 × (1/log4) ≈ 30 × 0.50 ≈ 15 分钟
- 第 10 次: 30 × (1/log11) ≈ 30 × 0.28 ≈ 8 分钟
- 文档化后: 查阅 2-5 分钟
- 模式化后: AI 自动避免 ≈ 0 分钟
```

---

## 七、总结：知识闭环的三个核心机制

### 机制 1：自动检索（减少踩坑）

```
plan 阶段 → learnings-researcher 自动搜索 docs/solutions/
         → 发现相关已解决问题
         → 在计划中引用解决方案
         → 实现时避免重复踩坑
```

### 机制 2：模式约束（保持一致性）

```
work 阶段 → 读取 docs/patterns/*.md + CLAUDE.md
         → AI 生成代码遵循模式
         → review 阶段检测违反
         → 发现违反 → 修复 + 可能强化模式文档
```

### 机制 3：持续积累（加速开发）

```
解决问题 → /workflows:compound 文档化
        → 单次解决方案 (docs/solutions/)
        → 出现 3+ 次 → 提升为模式 (docs/patterns/)
        → 高频/高风险 → 加入必读 (critical-patterns.md)
        → 项目强制 → 写入 CLAUDE.md
        → 下次开发自动复用
```

**核心理念**：每一次问题解决都应该让下一次同类问题的解决变得更快——这就是 Compound Engineering（复合工程）的本质。
