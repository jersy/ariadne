# Compound Engineering Plugin 组件系统详解

> **文档版本**: 基于 Compound Engineering Plugin v2.28.0
> **生成日期**: 2026-01-31

---

## 1. 组件系统概述

### 1.1 什么是组件系统?

**组件系统 = 代理(Agents) + 命令(Commands) + 技能(Skills)**

Compound Engineering Plugin 的核心是一个三层组件架构,每一层都有明确的职责和交互模式:

```
组件系统层次结构
────────────────────────────────────────

用户交互层 (Commands)
    ↓ 调用
代理执行层 (Agents)
    ↓ 使用
知识流程层 (Skills)
```

**61 个核心组件**:
- 27 个专业化代理
- 20 个工作流命令
- 14 个可复用技能

### 1.2 组件分类体系

```
📁 组件库 (61 个)
│
├── 🤖 代理系统 (27 个)
│   ├── review/        # 代码审查代理 (14)
│   ├── research/      # 研究分析代理 (4)
│   ├── design/        # 设计同步代理 (3)
│   ├── workflow/      # 工作流代理 (5)
│   └── docs/          # 文档生成代理 (1)
│
├── ⚡ 命令系统 (20 个)
│   ├── workflows/     # 核心工作流命令 (5)
│   └── utilities/     # 实用工具命令 (15)
│
└── 📚 技能系统 (14 个)
    ├── Architecture   # 架构设计技能 (3)
    ├── Development    # 开发实践技能 (3)
    ├── Content        # 内容创作技能 (2)
    ├── Automation     # 自动化工具技能 (3)
    ├── Testing        # 测试相关技能 (2)
    └── DevOps         # 运维部署技能 (1)
```

### 1.3 组件与工作流的关系

```
工作流阶段          命令                代理                    技能
────────────────────────────────────────────────────────────────
需求探索         /workflows:brainstorm  repo-research-analyst   brainstorming
实现规划         /workflows:plan        research agents         -
计划执行         /workflows:work        -                       git-worktree
代码审查         /workflows:review      review agents (14)      file-todos
知识复合         /workflows:compound    6 parallel subagents    -
```

---

## 2. 代理系统详解 (27 个)

### 2.1 代理架构

```
代理 = 专业化提示 + 执行上下文 + 输出格式

组成要素:
  • YAML Frontmatter (元数据)
  • 角色定义 (Role)
  • 审查原则 (Principles)
  • 执行流程 (Process)
  • 输出格式 (Output Format)
```

### 2.2 Review Agents (代码审查代理 - 14 个)

#### 2.2.1 语言特定审查代理 (3 个)

**kieran-rails-reviewer**

```yaml
名称: kieran-rails-reviewer
分类: review
用途: Rails 代码审查,遵循 Kieran 的严格标准
```

**核心原则**:

```
1. 现有代码修改 - 非常严格
   • 任何增加的复杂度需要强有力的理由
   • 总是优先提取到新控制器/服务
   • 问每个变更: "这会使现有代码更难理解吗?"

2. 新代码 - 务实
   • 如果隔离且有效,可以接受
   • 仍标记明显改进,但不阻止进展
   • 专注于代码可测试性和可维护性

3. Turbo Streams 约定
   • 简单 turbo streams 必须在控制器中内联数组
   • 🔴 FAIL: 简单操作使用单独的 .turbo_stream.erb 文件
   • ✅ PASS: render turbo_stream: [turbo_stream.replace(...)]

4. 测试作为质量指标
   对每个复杂方法问:
   • "我如何测试这个?"
   • "如果难以测试,应该提取什么?"
   • 难以测试的代码 = 需要重构的糟糕结构

5. 关键删除与回归
   对每个删除验证:
   • 这对此特定功能是有意的吗?
   • 删除这个会破坏现有工作流吗?
   • 有测试会失败吗?
   • 逻辑是移到别处还是完全删除?

6. 命名与清晰度 - 5 秒规则
   如果不能在 5 秒内从名称理解视图/组件的作用:
   • 🔴 FAIL: show_in_frame, process_stuff
   • ✅ PASS: fact_check_modal, _fact_frame

7. 服务提取信号
   考虑提取到服务当看到多个:
   • 复杂业务规则(不仅仅是"很长")
   • 多个模型一起编排
   • 外部 API 交互或复杂 I/O
   • 希望在控制器间重用的逻辑

8. 命名空间约定
   • 总是使用 class Module::ClassName 模式
   • 🔴 FAIL: module Assistant; class CategoryComponent
   • ✅ PASS: class Assistant::CategoryComponent

9. 核心哲学
   • 重复 > 复杂: "我宁愿有四个简单操作的控制器,也不要三个都自定义且非常复杂的控制器"
   • 简单、重复的易于理解的代码优于复杂的 DRY 抽象
   • "添加更多控制器从来不是坏事。使控制器非常复杂才是坏事"
   • 性能很重要: 总是考虑"规模化会怎样?"但如果还不是问题或没到规模,不添加缓存。保持简单 KISS
   • 平衡索引建议与提醒索引不是免费的 - 它们会减慢写入
```

**审查流程**:

```
1. 从最关键问题开始(回归、删除、破坏性变更)
2. 检查 Rails 约定违规
3. 评估可测试性和清晰度
4. 建议具体改进并提供示例
5. 对现有代码修改严格,对新隔离代码务实
6. 总是解释为什么某些东西不符合标准
```

**触发场景**:

```
✅ 使用:
  • 实现新功能后
  • 修改现有代码后
  • 创建新 Rails 组件后
  • PR 审查时

示例:
用户: "我在 posts 控制器中添加了新的 update 操作"
代理: "我已实现 update 操作。现在让 Kieran 审查此代码以确保符合质量标准。"
→ Task kieran-rails-reviewer(代码变更)
```

---

**kieran-python-reviewer**

```yaml
名称: kieran-python-reviewer
分类: review
用途: Python 代码审查,PEP 8 和 Pythonic 风格
```

**核心原则**:

```
1. PEP 8 遵从性
   • 命名约定(snake_case, PascalCase)
   • 行长度(79 字符代码,72 注释)
   • 导入顺序(标准库 → 第三方 → 本地)

2. Pythonic 风格
   • 列表推导式 vs 循环
   • 上下文管理器(with 语句)
   • 生成器 vs 列表
   • 鸭子类型 vs 类型检查

3. 类型提示
   • 关键函数需要类型注解
   • 使用 Optional, Union, List, Dict 等
   • 避免 Any 除非必要

4. 错误处理
   • 具体异常优于通用 Exception
   • 适当的异常链接
   • 清晰的错误消息

5. 性能考虑
   • 避免不必要的列表创建
   • 使用生成器处理大数据集
   • 适当的缓存策略

6. 测试覆盖
   • pytest 约定
   • 边缘案例覆盖
   • Mock 和 fixture 使用
```

---

**kieran-typescript-reviewer**

```yaml
名称: kieran-typescript-reviewer
分类: review
用途: TypeScript/JavaScript 代码审查
```

**核心原则**:

```
1. TypeScript 严格性
   • strict: true 配置
   • 避免 any,使用 unknown
   • 适当的类型守卫

2. 现代 JavaScript
   • async/await 优于 Promise 链
   • 可选链(?.)和空值合并(??)
   • 解构和展开操作符

3. React/Vue 最佳实践
   • 函数组件优于类组件
   • Hooks 规则遵从
   • 组件拆分和可重用性

4. 性能优化
   • useMemo 和 useCallback 适当使用
   • 虚拟化长列表
   • 代码分割和懒加载

5. 错误处理
   • Error boundaries
   • 优雅降级
   • 用户友好错误消息
```

#### 2.2.2 专业审查代理 (11 个)

**security-sentinel (安全哨兵)**

```yaml
名称: security-sentinel
分类: review/security
用途: 安全漏洞扫描和最佳实践验证
```

**审查清单**:

```
🔒 输入验证
  ├─ SQL 注入防护
  ├─ XSS 防护
  ├─ CSRF 令牌
  └─ 参数白名单

🔐 认证与授权
  ├─ 密码哈希(bcrypt, argon2)
  ├─ 会话管理
  ├─ 权限检查
  └─ API 令牌安全

🗝️ 数据保护
  ├─ 敏感数据加密
  ├─ PII 处理
  ├─ 日志脱敏
  └─ 安全存储

🌐 网络安全
  ├─ HTTPS 强制
  ├─ CORS 配置
  ├─ 安全头设置
  └─ 速率限制

📦 依赖安全
  ├─ 已知漏洞扫描
  ├─ 版本锁定
  └─ 供应链攻击防护
```

---

**performance-oracle (性能预言家)**

```yaml
名称: performance-oracle
分类: review/performance
用途: 性能问题识别和优化建议
```

**分析维度**:

```
⚡ 数据库性能
  ├─ N+1 查询检测
  ├─ 缺失索引识别
  ├─ 查询复杂度分析
  └─ 连接池配置

🔄 缓存策略
  ├─ 缓存机会识别
  ├─ 缓存失效策略
  ├─ 缓存键设计
  └─ 缓存层选择

📊 算法复杂度
  ├─ 时间复杂度评估
  ├─ 空间复杂度评估
  └─ 可扩展性分析

🌐 前端性能
  ├─ Bundle 大小
  ├─ 懒加载机会
  ├─ 渲染性能
  └─ 资源优化

🎯 API 性能
  ├─ 响应时间
  ├─ Payload 大小
  ├─ 分页策略
  └─ 并发处理
```

---

**architecture-strategist (架构策略师)**

```yaml
名称: architecture-strategist
分类: review/architecture
用途: 架构设计审查和改进建议
```

**审查视角**:

```
🏗️ 系统设计
  ├─ 模块化和解耦
  ├─ 关注点分离
  ├─ 依赖方向
  └─ 接口设计

📐 设计模式
  ├─ 模式适用性
  ├─ 过度工程检测
  ├─ 反模式识别
  └─ 重构机会

🔌 集成设计
  ├─ 外部服务集成
  ├─ 错误处理策略
  ├─ 重试和超时
  └─ 降级方案

📈 可扩展性
  ├─ 水平扩展能力
  ├─ 状态管理
  ├─ 并发处理
  └─ 资源管理

🔮 未来演进
  ├─ 技术债务识别
  ├─ 扩展点设计
  ├─ 迁移路径
  └─ 版本兼容性
```

---

**data-integrity-guardian (数据完整性守护者)**

```yaml
名称: data-integrity-guardian
分类: review/database
用途: 数据库设计和数据完整性审查
```

**审查要点**:

```
🗄️ 模式设计
  ├─ 规范化程度
  ├─ 外键约束
  ├─ 唯一约束
  └─ 检查约束

🔒 数据完整性
  ├─ 参照完整性
  ├─ 域完整性
  ├─ 实体完整性
  └─ 业务规则强制

🔄 迁移安全
  ├─ 向后兼容性
  ├─ 数据迁移策略
  ├─ 回滚计划
  └─ 零停机部署

📊 索引策略
  ├─ 索引覆盖
  ├─ 复合索引顺序
  ├─ 部分索引机会
  └─ 索引维护成本

⚠️ 常见问题
  ├─ 孤立记录
  ├─ 级联删除风险
  ├─ 竞态条件
  └─ 并发更新冲突
```

---

**其他专业审查代理**:

| 代理名称 | 用途 | 关键关注点 |
|---------|------|-----------|
| **accessibility-reviewer** | 可访问性审查 | WCAG 2.1, ARIA, 键盘导航, 屏幕阅读器 |
| **api-design-reviewer** | API 设计审查 | RESTful 原则, 版本控制, 错误处理, 文档 |
| **testing-reviewer** | 测试质量审查 | 覆盖率, 测试隔离, Mock 使用, 边缘案例 |
| **documentation-reviewer** | 文档质量审查 | 清晰度, 完整性, 示例, 更新性 |
| **ux-reviewer** | 用户体验审查 | 可用性, 一致性, 反馈, 错误处理 |
| **devops-reviewer** | DevOps 实践审查 | CI/CD, 监控, 日志, 部署策略 |
| **code-simplicity-reviewer** | 代码简洁性审查 | 复杂度, 可读性, YAGNI, 重复 |

### 2.3 Research Agents (研究分析代理 - 4 个)

**repo-research-analyst**

```yaml
名称: repo-research-analyst
分类: research
用途: 代码库模式分析和现有实现查找
```

**研究流程**:

```
Phase 1: 上下文理解
  ├─ 读取 CLAUDE.md (项目约定)
  ├─ 读取 README.md (项目概述)
  └─ 分析目录结构

Phase 2: 模式识别
  ├─ 查找类似功能实现
  ├─ 识别命名约定
  ├─ 分析代码组织模式
  └─ 提取技术栈信息

Phase 3: 最佳实践提取
  ├─ 项目特定约定
  ├─ 架构模式
  ├─ 测试策略
  └─ 错误处理模式

Phase 4: 结果汇总
  ├─ 相关文件路径
  ├─ 代码片段示例
  ├─ 约定总结
  └─ 建议引用
```

**输出格式**:

```markdown
## 代码库研究: [功能描述]

### 现有模式
- 类似实现: `app/models/existing_feature.rb`
- 测试模式: `test/models/existing_feature_test.rb`
- 控制器模式: `app/controllers/existing_controller.rb`

### 项目约定 (来自 CLAUDE.md)
- 命名约定: [约定描述]
- 架构模式: [模式描述]
- 测试要求: [要求描述]

### 技术栈
- 框架: Rails 7.1
- 数据库: PostgreSQL
- 前端: Turbo + Stimulus

### 建议
基于现有模式,建议遵循:
1. [建议 1]
2. [建议 2]
```

---

**learnings-researcher**

```yaml
名称: learnings-researcher
分类: research
用途: 搜索已记录的解决方案和经验教训
```

**搜索策略**:

```
搜索目标: docs/solutions/

搜索维度:
1. 问题类别匹配
   ├─ build-errors/
   ├─ test-failures/
   ├─ performance-issues/
   ├─ security-issues/
   └─ database-issues/

2. 关键词匹配
   ├─ 标题关键词
   ├─ 标签关键词
   └─ 内容关键词

3. 时间相关性
   ├─ 最近 3 个月: 高优先级
   ├─ 3-6 个月: 中优先级
   └─ 6+ 个月: 低优先级

4. 相似度评分
   ├─ 症状相似度
   ├─ 根因相似度
   └─ 解决方案适用性
```

**输出格式**:

```markdown
## 已记录学习: [查询描述]

### 相关解决方案
1. **[问题标题]** (docs/solutions/category/file.md)
   - 症状: [症状描述]
   - 根因: [根因描述]
   - 解决方案: [解决方案摘要]
   - 相关性: 85%

2. **[问题标题]** (docs/solutions/category/file.md)
   - 症状: [症状描述]
   - 根因: [根因描述]
   - 解决方案: [解决方案摘要]
   - 相关性: 72%

### 预防策略
- [策略 1]
- [策略 2]

### 测试用例参考
- [测试用例 1]
- [测试用例 2]
```

---

**best-practices-researcher**

```yaml
名称: best-practices-researcher
分类: research
用途: 查找行业最佳实践和标准
```

**研究来源**:

```
外部资源:
  ├─ 官方文档
  ├─ 行业标准(RFC, W3C)
  ├─ 知名博客和文章
  └─ 开源项目参考

研究主题:
  ├─ 设计模式
  ├─ 安全最佳实践
  ├─ 性能优化
  ├─ 测试策略
  └─ 架构模式
```

---

**framework-docs-researcher**

```yaml
名称: framework-docs-researcher
分类: research
用途: 搜索框架和库的官方文档
```

**支持框架**:

```
Web 框架:
  • Rails (guides.rubyonrails.org)
  • Django (docs.djangoproject.com)
  • Express (expressjs.com)

前端框架:
  • React (react.dev)
  • Vue (vuejs.org)
  • Angular (angular.io)

数据库:
  • PostgreSQL (postgresql.org/docs)
  • MySQL (dev.mysql.com/doc)
  • MongoDB (docs.mongodb.com)
```

### 2.4 Design Agents (设计同步代理 - 3 个)

**figma-design-sync**

```yaml
名称: figma-design-sync
分类: design
用途: Figma 设计与实现对比和同步
```

**同步流程**:

```
1. 获取 Figma 设计
   ├─ 解析 Figma URL
   ├─ 提取设计规范
   └─ 识别组件

2. 分析实现
   ├─ 捕获当前 UI 截图
   ├─ 提取样式属性
   └─ 识别差异

3. 差异报告
   ├─ 颜色差异
   ├─ 间距差异
   ├─ 字体差异
   └─ 布局差异

4. 修复建议
   ├─ CSS 调整
   ├─ 组件重构
   └─ 优先级排序
```

---

**ui-designer & design-system-manager**

| 代理 | 用途 | 核心功能 |
|------|------|---------|
| **ui-designer** | UI 设计生成 | 生成响应式布局、颜色方案、组件设计 |
| **design-system-manager** | 设计系统管理 | 维护设计令牌、组件库、样式指南 |

### 2.5 Workflow Agents (工作流代理 - 5 个)

**spec-flow-analyzer**

```yaml
名称: spec-flow-analyzer
分类: workflow
用途: 需求规范分析和缺口识别
```

**分析维度**:

```
✓ 功能完整性
  ├─ 核心功能覆盖
  ├─ 边缘案例考虑
  └─ 错误场景处理

✓ 验收标准
  ├─ 可测试性
  ├─ 可衡量性
  └─ 明确性

✓ 技术考虑
  ├─ 性能影响
  ├─ 安全影响
  └─ 可扩展性

✓ 依赖分析
  ├─ 前置条件
  ├─ 外部依赖
  └─ 并行可能性
```

---

**其他工作流代理**:

| 代理 | 用途 | 核心功能 |
|------|------|---------|
| **bug-reproduction-validator** | Bug 复现验证 | 验证复现步骤、环境配置、最小化测试用例 |
| **lint** | 代码风格检查 | 运行 linter、修复自动修复、报告问题 |
| **pr-comment-resolver** | PR 评论解决 | 提取评论、分类问题、生成修复任务 |
| **every-style-editor** | 文档风格编辑 | 风格一致性、清晰度改进、格式化 |

### 2.6 Docs Agents (文档生成代理 - 1 个)

**ankane-readme-writer**

```yaml
名称: ankane-readme-writer
分类: docs
用途: 生成 Andrew Kane 风格的 README
```

**README 结构**:

```markdown
# Project Name

[单句描述]

## Installation

[简洁安装步骤]

## Getting Started

[最简单使用示例]

## Features

- Feature 1
- Feature 2

## Advanced

[可选高级功能]

## Performance

[性能数据和优化]

## Credits

[致谢和贡献]
```

---

## 3. 命令系统详解 (20 个)

### 3.1 命令架构

```
命令 = 触发器 + 参数 + 执行流程 + 代理调度

组成要素:
  • YAML Frontmatter (name, description, argument-hint)
  • 执行阶段 (Phases)
  • 代理调度逻辑 (Agent Dispatch)
  • 输出格式 (Output Format)
```

### 3.2 Workflow Commands (核心工作流 - 5 个)

#### workflows:brainstorm

```yaml
名称: workflows:brainstorm
参数: [feature idea or problem to explore]
用途: 需求探索和设计讨论
```

**执行流程** (详见"02-核心工作流详解.md"):

```
Phase 0: 评估需求清晰度
  └─ 如果清晰 → 建议跳到 /workflows:plan

Phase 1: 理解想法
  ├─ 1.1 仓库研究 (轻量级)
  │   └─ Task repo-research-analyst()
  └─ 1.2 协作对话
      └─ 使用 AskUserQuestion 逐个提问

Phase 2: 探索方案
  └─ 提出 2-3 种方案,推荐一个

Phase 3: 捕获设计
  └─ 写入 docs/brainstorms/YYYY-MM-DD-<topic>-brainstorm.md

Phase 4: 交接
  └─ 提供下一步选项
```

**输出文件**:

```
docs/brainstorms/YYYY-MM-DD-<topic>-brainstorm.md
```

---

#### workflows:plan

```yaml
名称: workflows:plan
参数: [feature description, bug report, or improvement idea]
用途: 创建结构化实现计划
```

**执行流程** (详见"02-核心工作流详解.md"):

```
Step 0: 想法细化
  └─ 检查 docs/brainstorms/ 是否有相关设计

Step 1: 本地研究 (始终并行运行)
  ├─ Task repo-research-analyst()
  └─ Task learnings-researcher()

Step 1.5: 研究决策
  └─ 决定是否需要外部研究

Step 1.5b: 外部研究 (条件性)
  ├─ Task best-practices-researcher()
  └─ Task framework-docs-researcher()

Step 2: Issue 规划与结构
  └─ 确定类型、标题、文件名

Step 3: SpecFlow 分析
  └─ Task spec-flow-analyzer()

Step 4: 选择实现详细程度
  └─ MINIMAL / MORE / A LOT

Step 5: Issue 创建与格式化
  └─ 使用模板生成计划

Step 6: 最终审查与提交
  └─ 验证完整性
```

**输出文件**:

```
docs/plans/YYYY-MM-DD-<type>-<name>-plan.md
```

**计划模板**:

| 模板 | 适用场景 | 包含章节 |
|------|---------|---------|
| **MINIMAL** | 简单 bugs、小改进 | 问题描述 + 验收标准 + 基本上下文 |
| **MORE** | 大多数功能、团队协作 | MINIMAL + 详细背景 + 技术考虑 + 依赖 |
| **A LOT** | 主要功能、架构变更 | MORE + 实现阶段 + 替代方案 + 风险缓解 |

---

#### workflows:work

```yaml
名称: workflows:work
参数: [plan file, specification, or todo file path]
用途: 系统化执行工作项
```

**执行流程** (详见"02-核心工作流详解.md"):

```
Phase 1: 快速启动
  ├─ 读取计划并澄清
  ├─ 设置环境 (分支/worktree)
  └─ 创建待办列表 (TodoWrite)

Phase 2: 执行
  ├─ 任务执行循环
  ├─ 增量提交
  ├─ 遵循现有模式
  ├─ 持续测试
  ├─ Figma 设计同步 (如适用)
  └─ 跟踪进度

Phase 3: 质量检查
  ├─ 运行测试套件
  ├─ 运行 linting
  └─ 考虑审查代理 (复杂更改)

Phase 4: 交付
  ├─ 创建提交
  ├─ 捕获截图 (UI 更改)
  ├─ 创建 Pull Request
  └─ 通知用户
```

**质量检查清单**:

```
- [ ] 所有澄清问题已提问并回答
- [ ] 所有 TodoWrite 任务标记为已完成
- [ ] 测试通过
- [ ] Linting 通过
- [ ] 代码遵循现有模式
- [ ] Figma 设计匹配实现 (如适用)
- [ ] 前后截图已捕获并上传 (UI 更改)
- [ ] 提交消息遵循常规格式
- [ ] PR 描述包含摘要、测试和截图
```

---

#### workflows:review

```yaml
名称: workflows:review
参数: [PR number, GitHub URL, branch name, or latest]
用途: 穷尽式多代理代码审查
```

**执行流程** (详见"02-核心工作流详解.md"):

```
Step 1: 确定审查目标并设置
  ├─ 确定审查类型
  ├─ 检查当前 git 分支
  ├─ 提供 worktree 选项
  └─ 获取 PR 元数据

Step 2: 并行代理审查 (12 个核心代理同时)
  ├─ kieran-rails-reviewer
  ├─ dhh-rails-reviewer
  ├─ security-sentinel
  ├─ performance-oracle
  ├─ architecture-strategist
  ├─ code-philosopher
  ├─ git-history-analyzer
  ├─ dependency-detective
  ├─ pattern-recognition-specialist
  ├─ devops-harmony-analyst
  ├─ data-integrity-guardian
  └─ agent-native-reviewer

Step 3: 条件性代理
  ├─ IF 有迁移 → data-migration-expert
  ├─ IF 有迁移 → deployment-verification-agent
  └─ IF 使用 Turbo → rails-turbo-expert

Step 4: 超深度思考分析
  ├─ 利益相关者视角分析
  ├─ 场景探索 (边缘案例)
  └─ Task code-simplicity-reviewer()

Step 5: 发现综合和待办创建
  ├─ 按类型分类
  ├─ 分配严重性 (P1/P2/P3)
  ├─ 使用 file-todos skill 创建待办文件
  └─ 摘要报告

Step 6: 端到端测试 (可选)
  ├─ 检测项目类型
  └─ 提供测试选项
```

**优先级级别**:

```
🔴 P1 (CRITICAL - 阻止合并)
   ├─ 安全漏洞
   ├─ 数据损坏风险
   ├─ 破坏性更改
   └─ 关键架构问题

🟡 P2 (IMPORTANT - 应该修复)
   ├─ 性能问题
   ├─ 重要的架构关注
   ├─ 主要代码质量问题
   └─ 可靠性问题

🔵 P3 (NICE-TO-HAVE)
   ├─ 小改进
   ├─ 代码清理
   ├─ 优化机会
   └─ 文档更新
```

**输出文件**:

```
todos/
├── 001-pending-p1-{finding}.md
├── 002-pending-p2-{finding}.md
└── 003-pending-p3-{finding}.md
```

---

#### workflows:compound

```yaml
名称: workflows:compound
参数: [optional: brief context about the fix]
用途: 文档化已解决问题,知识复合
```

**执行流程** (详见"02-核心工作流详解.md"):

```
并行子代理执行 (6 个同时):

1. Task context-analyzer(对话历史)
   → 返回: YAML frontmatter 骨架

2. Task solution-extractor(调查步骤)
   → 返回: 解决方案内容块

3. Task related-docs-finder()
   → 返回: 链接和关系

4. Task prevention-strategist()
   → 返回: 预防/测试内容

5. Task category-classifier()
   → 返回: 最终路径和文件名

6. Task documentation-writer()
   → 返回: 创建文件

专业代理审查 (基于问题类型自动触发):
  ├─ IF performance_issue → performance-oracle
  ├─ IF security_issue → security-sentinel
  ├─ IF database_issue → data-integrity-guardian
  └─ 任何代码密集型问题 → kieran-rails-reviewer + code-simplicity-reviewer
```

**输出文件**:

```
docs/solutions/[category]/[filename].md

自动检测的类别:
├─ build-errors/
├─ test-failures/
├─ runtime-errors/
├─ performance-issues/
├─ database-issues/
├─ security-issues/
├─ ui-bugs/
├─ integration-issues/
└─ logic-errors/
```

**解决方案文档模板**:

```markdown
---
title: [问题标题]
category: [auto-detected category]
date: YYYY-MM-DD
tags: [relevant, tags, here]
related_issues: [#123, #456]
---

# [问题标题]

## 症状
[确切错误消息和可观察行为]

## 根本原因
[技术解释]

## 解决方案
### 步骤 1: [描述]
[代码示例]

## 预防
- [ ] 最佳实践 1
- [ ] 最佳实践 2

## 测试
[测试用例防止回归]

## 相关资源
- [相关文档](URL)
- GitHub Issues: #123
```

### 3.3 Utility Commands (实用工具 - 15 个)

#### 规划增强命令

**deepen-plan**

```yaml
名称: deepen-plan
用途: 通过并行研究代理增强计划每个章节
```

**执行流程**:

```
1. 读取现有计划文档
2. 识别需要深化的章节
3. 为每个章节派遣专业研究代理
   ├─ 技术章节 → framework-docs-researcher
   ├─ 安全章节 → best-practices-researcher
   └─ 性能章节 → performance-oracle
4. 整合研究结果到计划
5. 更新计划文档
```

---

**plan_review**

```yaml
名称: plan_review
用途: 从审查者获得计划反馈
```

**审查维度**:

```
✓ 完整性
  ├─ 所有必需章节存在
  ├─ 验收标准清晰
  └─ 技术细节充分

✓ 可行性
  ├─ 实现可行
  ├─ 时间估算合理
  └─ 资源可用

✓ 风险
  ├─ 风险识别
  ├─ 缓解策略
  └─ 应急计划
```

---

#### 待办管理命令

**triage**

```yaml
名称: triage
用途: 分类和优先级排序待办事项
```

**分类流程**:

```
对每个 todos/*-pending-*.md:

1. 读取待办内容
2. 评估优先级 (P1/P2/P3)
3. 评估工作量 (Small/Medium/Large)
4. 决策:
   ├─ 批准 → 重命名为 *-ready-*.md
   ├─ 延期 → 添加注释,保留 pending
   └─ 拒绝 → 删除文件
5. 更新依赖关系
```

---

**resolve_todo_parallel**

```yaml
名称: resolve_todo_parallel
用途: 并行解决多个待办事项
```

**执行流程**:

```
1. 扫描 todos/*-ready-*.md 文件
2. 按优先级排序 (P1 → P2 → P3)
3. 派遣并行子代理修复
   ├─ P1 任务: 立即并行执行
   ├─ P2 任务: 批次并行执行
   └─ P3 任务: 可选并行执行
4. 验证修复
5. 重命名为 *-complete-*.md
6. 提交更改
```

---

**resolve_pr_parallel**

```yaml
名称: resolve_pr_parallel
用途: 并行解决 PR 评论
```

**执行流程**:

```
1. 获取 PR 评论 (gh pr view --json comments)
2. 分类评论:
   ├─ 需要代码更改
   ├─ 需要说明
   └─ 已解决
3. 为每个代码更改评论创建子任务
4. 并行执行修复
5. 回复评论
6. 推送更改
```

---

#### 测试与质量命令

**test-browser**

```yaml
名称: test-browser
用途: CLI 浏览器自动化测试
```

**测试流程**:

```
1. 启动开发服务器
2. 使用 agent-browser 打开页面
3. 执行测试步骤:
   ├─ 导航到页面
   ├─ 填写表单
   ├─ 点击按钮
   └─ 验证结果
4. 捕获截图
5. 生成测试报告
```

---

**xcode-test**

```yaml
名称: xcode-test
用途: Xcode 模拟器测试
```

**测试流程**:

```
1. 构建应用
2. 启动模拟器
3. 运行 UI 测试
4. 捕获截图
5. 生成测试报告
```

---

#### 文档生成命令

**changelog**

```yaml
名称: changelog
用途: 生成版本变更日志
```

**生成流程**:

```
1. 读取 git log 自上次发布
2. 按类型分类提交:
   ├─ feat: 新功能
   ├─ fix: Bug 修复
   ├─ refactor: 重构
   ├─ docs: 文档
   └─ chore: 杂项
3. 生成 CHANGELOG.md
4. 遵循 Keep a Changelog 格式
```

---

**deploy-docs & release-docs**

```yaml
名称: deploy-docs, release-docs
用途: 文档部署和发布
```

**部署流程**:

```
1. 构建文档站点
2. 更新统计数据
3. 重新生成参考页面
4. 部署到 GitHub Pages
```

---

#### 其他实用命令

| 命令 | 用途 | 核心功能 |
|------|------|---------|
| **reproduce-bug** | Bug 复现 | 最小化复现步骤、环境配置、验证 |
| **report-bug** | Bug 报告 | 收集环境信息、日志、截图 |
| **agent-native-audit** | 代理原生审计 | 审计 AI 代理实现质量 |
| **create-agent-skill** | 创建代理/技能 | 脚手架生成、模板填充 |
| **heal-skill** | 修复技能 | 验证技能格式、修复错误 |
| **feature-video** | 功能演示视频 | 录制功能演示、生成教程 |
| **lfg** | 快速开始 | "Let's F**king Go" - 快速项目启动 |

---

## 4. 技能系统详解 (14 个)

### 4.1 技能架构

```
技能 = 流程知识 + 最佳实践 + 可复用模板

组成要素:
  • YAML Frontmatter (name, description)
  • 何时使用 (When to Use)
  • 核心流程 (Core Process)
  • 最佳实践 (Best Practices)
  • 反模式 (Anti-patterns)
  • 辅助资源 (Supporting Resources)
```

**技能 vs 命令 vs 代理**:

```
技能 (Skill)         命令 (Command)       代理 (Agent)
──────────────────   ───────────────────  ──────────────────
流程知识              触发器               执行者
"如何做"             "调用什么"           "谁来做"
可复用模板            用户界面             专业化提示
引用和指导            编排和调度           分析和输出
```

### 4.2 Architecture Skills (架构设计 - 3 个)

#### agent-native-architecture

```yaml
名称: agent-native-architecture
分类: architecture
用途: AI 代理驱动的架构设计模式
```

**核心概念**:

```
代理原生架构原则:

1. 文件作为通用接口
   ├─ 所有知识在文件中
   ├─ 代理读写文件
   └─ 无隐藏状态

2. 动态上下文注入
   ├─ CLAUDE.md 动态加载
   ├─ 技能按需加载
   └─ 上下文自适应

3. 从原语到领域工具
   ├─ 低级工具 (Read, Write, Bash)
   ├─ 中级工具 (TodoWrite, AskUserQuestion)
   └─ 高级工具 (Task, Skill)

4. 操作平等性纪律
   ├─ 代理操作 = 人类操作
   ├─ 可验证性
   └─ 可重现性

5. 代理执行模式
   ├─ 主代理 + 子代理
   ├─ 并行执行
   └─ 结果聚合
```

**参考资源**:

```
references/
├── action-parity-discipline.md
├── agent-execution-patterns.md
├── agent-native-testing.md
├── architecture-patterns.md
├── dynamic-context-injection.md
├── files-universal-interface.md
├── from-primitives-to-domain-tools.md
├── mcp-tool-design.md
├── mobile-patterns.md
├── product-implications.md
├── refactoring-to-prompt-native.md
├── self-modification.md
├── shared-workspace-architecture.md
└── system-prompt-design.md
```

---

#### dhh-rails-style

```yaml
名称: dhh-rails-style
分类: architecture
用途: DHH (David Heinemeier Hansson) Rails 代码风格
```

**核心原则**:

```
1. 约定优于配置
   • 遵循 Rails 约定
   • 最小化配置文件
   • 标准目录结构

2. 提取服务的时机
   • 复杂业务规则
   • 多模型编排
   • 外部集成
   • 跨控制器重用

3. 控制器设计
   • 简单 RESTful 操作
   • 瘦控制器
   • 标准 7 个操作
   • 提取到服务优于复杂控制器

4. 测试哲学
   • 系统测试优先
   • 关键单元测试
   • 避免过度 mock
   • 真实数据库测试

5. Turbo 和 Hotwire
   • 内联 turbo_stream
   • 避免单独模板文件
   • 服务器渲染优先
```

**参考资源**:

```
references/
└── controllers.md  # 控制器设计模式
```

---

#### dspy-ruby

```yaml
名称: dspy-ruby
分类: architecture
用途: 类型安全 LLM 应用开发 (Ruby)
```

**核心概念**:

```
1. 声明式提示
   • 模式定义
   • 类型验证
   • 结构化输出

2. 组合模式
   • 提示链接
   • 并行执行
   • 结果聚合

3. 最佳实践
   • 版本控制
   • 测试策略
   • 错误处理
```

### 4.3 Development Skills (开发实践 - 3 个)

#### brainstorming

```yaml
名称: brainstorming
分类: development
用途: 需求探索和设计讨论
```

**核心流程** (详见技能文件):

```
Phase 0: 评估需求清晰度
Phase 1: 理解想法
Phase 2: 探索方案
Phase 3: 捕获设计
```

**问题技巧**:

```
1. 优先多选题
   ✅ "通知方式: (a) 邮件 (b) 应用内 (c) 两者?"
   ❌ "如何通知用户?"

2. 从广到窄
   ├─ 核心目的
   ├─ 用户群体
   └─ 约束条件

3. 明确验证假设
   "我假设用户已登录。对吗?"

4. 早期询问成功标准
   "如何知道功能运作良好?"
```

---

#### frontend-design

```yaml
名称: frontend-design
分类: development
用途: 生产级 UI 界面设计
```

**设计原则**:

```
1. 响应式设计
   ├─ 移动优先
   ├─ 断点策略
   └─ 灵活布局

2. 可访问性
   ├─ WCAG 2.1 AA
   ├─ 键盘导航
   └─ 屏幕阅读器

3. 性能
   ├─ 懒加载
   ├─ 图片优化
   └─ 代码分割

4. 一致性
   ├─ 设计系统
   ├─ 组件库
   └─ 样式指南
```

---

#### git-worktree

```yaml
名称: git-worktree
分类: development
用途: Git worktree 隔离工作区管理
```

**核心价值**:

```
Worktrees = 隔离 + 并行 + 安全

好处:
✅ 独立工作区,状态隔离
✅ 清洁测试基线
✅ 可并行开发多功能
✅ 随时切换,无需 stash
✅ 避免主分支污染
```

**工作流**:

```bash
# 1. 创建 worktree
git worktree add ../feature-branch feature/new-feature

# 2. 在 worktree 中工作
cd ../feature-branch
# ... 开发 ...

# 3. 清理 worktree
git worktree remove ../feature-branch
```

### 4.4 Content Skills (内容创作 - 2 个)

#### every-style-editor

```yaml
名称: every-style-editor
分类: content
用途: Every 公司风格文档编辑
```

**风格指南**:

```
1. 清晰简洁
   • 短句优于长句
   • 主动语态优于被动语态
   • 具体词汇优于模糊词汇

2. 结构化
   • 标题层次清晰
   • 列表和表格
   • 代码块和示例

3. 一致性
   • 术语统一
   • 格式统一
   • 风格统一
```

---

#### compound-docs

```yaml
名称: compound-docs
分类: content
用途: Compound Engineering 文档写作
```

**文档类型**:

```
1. 设计文档 (docs/brainstorms/)
   └─ 探索决策和方案

2. 计划文档 (docs/plans/)
   └─ 实现步骤和验收标准

3. 解决方案文档 (docs/solutions/)
   └─ 问题、根因、解决方案

4. TODO 文档 (todos/)
   └─ 待办事项和跟踪
```

### 4.5 Automation Skills (自动化工具 - 3 个)

#### agent-browser

```yaml
名称: agent-browser
分类: automation
用途: CLI 浏览器自动化
```

**核心命令**:

```bash
# 打开页面
agent-browser open https://example.com

# 捕获快照
agent-browser snapshot -i

# 截图
agent-browser screenshot output.png

# 填写表单
agent-browser fill "#email" "user@example.com"

# 点击元素
agent-browser click "button[type=submit]"

# 等待元素
agent-browser wait-for ".success-message"
```

---

#### rclone

```yaml
名称: rclone
分类: automation
用途: 云文件上传和同步
```

**支持的云服务**:

```
• Amazon S3
• Google Drive
• Dropbox
• OneDrive
• Backblaze B2
• DigitalOcean Spaces
• 等 40+ 云服务
```

---

#### gemini-imagegen

```yaml
名称: gemini-imagegen
分类: automation
用途: Gemini AI 图像生成和编辑
```

**功能**:

```
1. 文本到图像
   • 自然语言描述
   • 风格控制
   • 尺寸选择

2. 图像编辑
   • 局部编辑
   • 风格转换
   • 背景移除

3. 批量生成
   • 多变体
   • 并行处理
```

### 4.6 Testing & DevOps Skills

| 技能 | 分类 | 用途 |
|------|------|------|
| **file-todos** | testing | 结构化待办事项管理 |
| **create-agent-skills** | testing | 创建和测试代理/技能 |
| **skill-creator** | devops | 技能创作和验证工具 |

---

## 5. 组件文件结构与格式

### 5.1 统一目录结构

```
plugins/compound-engineering/
├── agents/
│   ├── review/
│   │   ├── kieran-rails-reviewer.md
│   │   ├── security-sentinel.md
│   │   └── ...
│   ├── research/
│   │   ├── repo-research-analyst.md
│   │   └── ...
│   ├── design/
│   │   └── figma-design-sync.md
│   ├── workflow/
│   │   └── spec-flow-analyzer.md
│   └── docs/
│       └── ankane-readme-writer.md
│
├── commands/
│   ├── workflows/
│   │   ├── brainstorm.md
│   │   ├── plan.md
│   │   ├── work.md
│   │   ├── review.md
│   │   └── compound.md
│   ├── deepen-plan.md
│   ├── triage.md
│   └── ...
│
└── skills/
    ├── agent-native-architecture/
    │   ├── SKILL.md
    │   └── references/
    │       ├── action-parity-discipline.md
    │       └── ...
    ├── brainstorming/
    │   └── SKILL.md
    └── ...
```

### 5.2 Agent 文件格式

**AGENT.md 结构**:

```yaml
---
name: agent-name
description: "Agent description with examples..."
model: inherit  # 可选: 指定模型
---

# Agent Role Definition

You are [role], a [expertise] with [characteristics].

## Core Principles

1. Principle 1
   • Detail 1
   • Detail 2

2. Principle 2
   • Detail 1
   • Detail 2

## Review/Analysis Process

1. Step 1
2. Step 2
3. Step 3

## Output Format

[Expected output structure]
```

**Frontmatter 字段**:

| 字段 | 必需 | 类型 | 说明 |
|------|------|------|------|
| `name` | ✅ | string | 代理唯一标识符 (lowercase-with-hyphens) |
| `description` | ✅ | string | 代理描述,包含触发场景和示例 |
| `model` | ❌ | string | 指定模型 (inherit 或 claude-opus-4-5) |

**Description 格式要求**:

```
结构:
1. 用途说明 (何时使用此代理)
2. 触发场景 (具体例子)
3. 示例对话 (包含 <example> 和 <commentary>)

示例:
"Use this agent when you need to review Rails code changes...

Examples:
- <example>
  Context: The user has just implemented a new controller action.
  user: \"I've added a new update action\"
  assistant: \"Let me have Kieran review this code...\"
  <commentary>
  Since new controller code was written, use kieran-rails-reviewer...
  </commentary>
  </example>"
```

### 5.3 Command 文件格式

**COMMAND.md 结构**:

```yaml
---
name: command-name  # 或 namespace:command-name
description: Command description
argument-hint: "[what the user should provide]"  # 可选
---

# Command Title

Brief description of what this command does.

## Arguments

[Description of arguments if any]

## Execution Flow

### Phase 1: [Phase Name]

[Detailed steps]

### Phase 2: [Phase Name]

[Detailed steps]

## Output

[What the command produces]

## Examples

[Usage examples]
```

**Frontmatter 字段**:

| 字段 | 必需 | 类型 | 说明 |
|------|------|------|------|
| `name` | ✅ | string | 命令名称 (workflows:plan 或 command-name) |
| `description` | ✅ | string | 命令描述 |
| `argument-hint` | ❌ | string | 参数提示 (显示在 UI 中) |

**命名约定**:

```
✅ 工作流命令: workflows:brainstorm, workflows:plan
   (避免与内置命令冲突)

✅ 实用命令: deepen-plan, triage, test-browser
   (简短、描述性)

❌ 避免: /plan (与内置冲突)
```

### 5.4 Skill 文件格式

**SKILL.md 结构**:

```yaml
---
name: skill-name
description: This skill should be used when... (第三人称)
---

# Skill Title

## When to Use This Skill

✅ Use when:
- Scenario 1
- Scenario 2

❌ Skip when:
- Scenario 3
- Scenario 4

## Core Process

### Phase 1: [Phase Name]

[Detailed steps]

### Phase 2: [Phase Name]

[Detailed steps]

## Best Practices

✅ Do:
- Practice 1
- Practice 2

❌ Don't:
- Anti-pattern 1
- Anti-pattern 2

## Supporting Resources

- [resource-1.md](./references/resource-1.md)
- [template.md](./assets/template.md)
- [script.sh](./scripts/script.sh)
```

**Frontmatter 字段**:

| 字段 | 必需 | 类型 | 说明 |
|------|------|------|------|
| `name` | ✅ | string | 技能名称 (lowercase-with-hyphens) |
| `description` | ✅ | string | **第三人称**描述 ("This skill should be used...") |

**Description 格式要求**:

```
✅ 正确 (第三人称):
"This skill should be used when exploring requirements..."
"This skill provides detailed process knowledge for..."

❌ 错误 (第二人称):
"Use this skill when you need to explore..."
"You should use this skill for..."
```

**引用链接要求**:

```
✅ 正确 (Markdown 链接):
- [filename.md](./references/filename.md)
- [script.sh](./scripts/script.sh)

❌ 错误 (裸反引号):
- `references/file.md`
- See `scripts/script.sh`
```

**目录结构**:

```
skills/skill-name/
├── SKILL.md              # 必需: 主技能文档
├── references/           # 可选: 参考文档
│   ├── guide-1.md
│   └── guide-2.md
├── assets/               # 可选: 模板和资源
│   ├── template.md
│   └── checklist.md
└── scripts/              # 可选: 可执行脚本
    └── helper.sh
```

### 5.5 格式验证清单

**Agent 验证**:

```bash
# 检查 frontmatter
grep -E '^name:|^description:|^model:' agents/**/*.md

# 检查示例格式
grep -E '<example>|<commentary>' agents/**/*.md

# 验证无语法错误
for f in agents/**/*.md; do
  yq eval '.name' "$f" >/dev/null || echo "Error in $f"
done
```

**Command 验证**:

```bash
# 检查 frontmatter
grep -E '^name:|^description:|^argument-hint:' commands/**/*.md

# 检查命名冲突
grep -E '^name: (plan|review|commit)$' commands/**/*.md
# 应返回空 (避免与内置命令冲突)
```

**Skill 验证**:

```bash
# 检查描述格式 (应使用第三人称)
grep -E '^description:' skills/**/SKILL.md | grep -v 'This skill'
# 应返回空

# 检查未链接引用
grep -E '`(references|assets|scripts)/[^`]+`' skills/**/SKILL.md
# 应返回空 (所有引用应是 Markdown 链接)

# 检查引用文件存在
for skill in skills/*/; do
  skill_name=$(basename "$skill")
  # 提取所有引用链接并验证文件存在
  grep -oE '\[.*\]\(\./[^)]+\)' "$skill/SKILL.md" | \
    sed 's/.*(\.\///' | sed 's/).*//' | \
    while read ref; do
      [[ -f "$skill/$ref" ]] || echo "Missing: $skill/$ref"
    done
done
```

---

## 6. 组件发现与加载机制

### 6.1 三级优先级系统

```
组件查找顺序 (从高到低):

1️⃣ 个人组件目录 (最高优先级)
   Claude Code:
   └─ ~/.claude/
      ├── agents/
      ├── commands/
      └── skills/

   OpenCode:
   └─ ~/.opencode/
      ├── agents/
      └── skills/

   Codex:
   └─ ~/.codex/
      ├── prompts/  (代理)
      └── skills/

   理由: 个人定制,覆盖所有其他组件

2️⃣ 项目组件目录 (中优先级)
   .opencode/
   ├── agents/
   └── skills/

   ./
   ├── agents/
   ├── commands/
   └── skills/

   理由: 项目特定,覆盖官方默认

3️⃣ 插件官方组件 (默认)
   <plugin-root>/
   ├── agents/
   ├── commands/
   └── skills/

   理由: 默认行为,最低优先级
```

**遮蔽规则**:

```
场景: 同名组件 "brainstorming"

~/.claude/skills/brainstorming/SKILL.md         (个人)
.opencode/skills/brainstorming/SKILL.md         (项目)
plugin/skills/brainstorming/SKILL.md            (官方)

加载结果: ~/.claude/skills/brainstorming/SKILL.md
理由: 个人 > 项目 > 官方
```

### 6.2 组件发现算法

```typescript
function discoverComponents(type: 'agents' | 'commands' | 'skills') {
  const searchPaths = [
    // 1. 个人目录
    `${HOME}/.claude/${type}/`,
    `${HOME}/.opencode/${type}/`,
    `${HOME}/.codex/${type === 'agents' ? 'prompts' : type}/`,

    // 2. 项目目录
    `${CWD}/.opencode/${type}/`,
    `${CWD}/${type}/`,

    // 3. 插件目录
    `${PLUGIN_ROOT}/${type}/`,
  ];

  const components = new Map<string, Component>();

  // 从低到高优先级扫描 (后加载的覆盖先加载的)
  for (const path of searchPaths.reverse()) {
    if (exists(path)) {
      const found = scanDirectory(path, type);
      for (const component of found) {
        // 同名组件会被覆盖 (高优先级覆盖低优先级)
        components.set(component.name, component);
      }
    }
  }

  return Array.from(components.values());
}
```

### 6.3 组件调用语法

**Agent 调用**:

```bash
# 方式 1: 直接使用 (在命令中)
Task kieran-rails-reviewer(PR内容)

# 方式 2: 手动调用 (用户命令)
claude agent kieran-rails-reviewer "审查此代码"

# 方式 3: 在文档中引用
参见 kieran-rails-reviewer 代理进行审查
```

**Command 调用**:

```bash
# 方式 1: 斜杠命令 (用户界面)
/workflows:brainstorm 添加评论功能

# 方式 2: 在代码中引用
运行 /workflows:plan 创建实现计划

# 方式 3: 工作流链接
/workflows:brainstorm → /workflows:plan → /workflows:work
```

**Skill 调用**:

```bash
# 方式 1: 显式加载
skill: brainstorming

# 方式 2: 在命令中引用
参见 `brainstorming` skill 了解问题技巧

# 方式 3: 在文档中链接
[brainstorming skill](./skills/brainstorming/SKILL.md)
```

### 6.4 动态上下文注入

```
组件加载时机:

🔄 会话启动时
  ├─ 加载 CLAUDE.md (项目约定)
  ├─ 扫描可用组件
  └─ 构建组件索引

🔄 命令执行时
  ├─ 解析命令名称
  ├─ 加载命令文档
  ├─ 注入命令上下文
  └─ 执行命令流程

🔄 代理派遣时
  ├─ 解析代理名称
  ├─ 加载代理提示
  ├─ 注入代理角色
  └─ 执行代理任务

🔄 技能引用时
  ├─ 解析技能名称
  ├─ 加载技能文档
  ├─ 注入流程知识
  └─ 遵循技能指导
```

**上下文注入示例**:

```
用户: /workflows:brainstorm 添加评论功能

系统内部:
1. 解析命令: workflows:brainstorm
2. 查找命令文件: commands/workflows/brainstorm.md
3. 读取命令内容
4. 注入到上下文:
   - 命令描述
   - 执行流程
   - 参数提示
5. 命令文档提到 "brainstorming" skill
6. 加载技能文件: skills/brainstorming/SKILL.md
7. 注入技能知识:
   - 何时使用
   - 核心流程
   - 最佳实践
8. 执行命令,遵循技能指导
```

---

## 7. 优先级系统与遮蔽规则

### 7.1 遮蔽场景

**场景 1: 修改官方代理**

```
需求: 调整 kieran-rails-reviewer 更严格

步骤:
1. 复制官方代理
   cp plugins/compound-engineering/agents/review/kieran-rails-reviewer.md \
      ~/.claude/agents/kieran-rails-reviewer.md

2. 修改个人副本
   vim ~/.claude/agents/kieran-rails-reviewer.md
   # 添加额外原则或调整现有原则

3. 验证遮蔽生效
   claude agent kieran-rails-reviewer "test"
   # 应使用个人版本

结果: 个人版本覆盖官方版本,项目继续使用官方版本
```

**场景 2: 项目特定技能**

```
需求: 为当前项目创建部署技能

步骤:
1. 创建项目技能目录
   mkdir -p .opencode/skills/deploy-workflow

2. 创建 SKILL.md
   cat > .opencode/skills/deploy-workflow/SKILL.md <<EOF
   ---
   name: deploy-workflow
   description: This skill should be used when deploying this project
   ---

   # Deploy Workflow

   ## Core Process
   1. Run tests: npm test
   2. Build: npm run build
   3. Deploy: ./scripts/deploy.sh
   EOF

3. 仅此项目可用
   # 其他项目不受影响

结果: 仅在此项目目录下可用此技能
```

**场景 3: 临时禁用组件**

```
需求: 临时禁用某个官方代理

方法 1: 创建空覆盖
~/.claude/agents/unwanted-agent.md
---
name: unwanted-agent
description: Disabled
---
# This agent is disabled

方法 2: 重命名官方组件 (不推荐)
mv plugins/.../agent.md plugins/.../agent.md.disabled

推荐: 使用方法 1 (不修改官方文件)
```

### 7.2 优先级决策表

| 位置 | 个人 | 项目 | 官方 | 加载? | 理由 |
|------|------|------|------|-------|------|
| ~/.claude/ | ✅ | ❌ | ❌ | **个人** | 最高优先级 |
| .opencode/ | ❌ | ✅ | ❌ | **项目** | 覆盖官方 |
| plugin/ | ❌ | ❌ | ✅ | **官方** | 默认 |
| ~/.claude/ + .opencode/ | ✅ | ✅ | ❌ | **个人** | 个人覆盖项目 |
| ~/.claude/ + plugin/ | ✅ | ❌ | ✅ | **个人** | 个人覆盖官方 |
| .opencode/ + plugin/ | ❌ | ✅ | ✅ | **项目** | 项目覆盖官方 |
| 全部存在 | ✅ | ✅ | ✅ | **个人** | 个人最高 |

### 7.3 版本控制策略

**个人组件** (~/.claude/):

```
版本控制: 可选
  ├─ 优点: 跨机器同步
  ├─ 缺点: 可能包含敏感信息
  └─ 建议: 使用 dotfiles 仓库,小心 .gitignore

示例:
~/dotfiles/
├── .claude/
│   ├── agents/
│   │   └── my-custom-agent.md
│   └── skills/
│       └── my-workflow/SKILL.md
└── .gitignore
    # 忽略敏感组件
    .claude/agents/proprietary-*.md
```

**项目组件** (.opencode/):

```
版本控制: 推荐
  ├─ 优点: 团队共享,项目特定
  ├─ 缺点: 无
  └─ 建议: 始终提交到 git

.gitignore 建议:
# 不要忽略项目组件
!.opencode/
!.opencode/agents/
!.opencode/skills/

# 但忽略临时文件
.opencode/*.log
.opencode/.cache/
```

**官方组件** (plugin/):

```
版本控制: 插件管理
  ├─ 不直接修改
  ├─ 使用遮蔽覆盖
  └─ 通过插件更新
```

---

## 8. 组件互操作性模式

### 8.1 命令 → 代理调度

**模式 1: 单一代理调度**

```
命令: /workflows:brainstorm
  ↓
调度逻辑:
  Task repo-research-analyst(feature_description)
  ↓
代理执行:
  • 读取 CLAUDE.md
  • 查找类似功能
  • 返回研究结果
  ↓
命令继续:
  • 使用研究结果
  • 继续协作对话
```

**模式 2: 并行代理调度**

```
命令: /workflows:plan
  ↓
并行调度:
  Task repo-research-analyst(description)
  Task learnings-researcher(description)
  ↓
等待所有代理完成
  ↓
聚合结果:
  • 本地研究 + 已记录学习
  • 整合到计划文档
```

**模式 3: 条件代理调度**

```
命令: /workflows:review
  ↓
核心代理 (始终):
  12 个并行代理
  ↓
条件代理 (基于 PR 内容):
  IF 有迁移文件:
    Task data-migration-expert()
    Task deployment-verification-agent()
  IF 使用 Turbo:
    Task rails-turbo-expert()
```

### 8.2 代理 → 技能引用

**模式 1: 显式技能加载**

```
命令文档:
"Load the `brainstorming` skill for detailed question techniques."

执行:
1. 解析技能引用: brainstorming
2. 查找技能文件: skills/brainstorming/SKILL.md
3. 读取技能内容
4. 注入到代理上下文
5. 代理遵循技能指导
```

**模式 2: 技能文档链接**

```
技能文档:
"See [action-parity-discipline.md](./references/action-parity-discipline.md)"

执行:
1. 代理读取主技能文档
2. 识别参考链接
3. 按需读取参考文档
4. 整合知识
```

### 8.3 命令 → 命令链接

**模式 1: 顺序工作流**

```
/workflows:brainstorm
  ↓ (设计批准后)
提示用户: "继续规划?"
  ↓ (用户确认)
/workflows:plan
  ↓ (计划批准后)
提示选项: "启动 /workflows:work?"
  ↓ (用户选择)
/workflows:work
  ↓ (PR 创建后)
建议: "运行 /workflows:review [PR]"
```

**模式 2: 自动触发**

```
用户: "that worked" / "it's fixed"
  ↓
系统检测触发短语
  ↓
自动运行: /workflows:compound [context]
  ↓
文档化解决方案
```

**模式 3: 并行命令**

```
命令: /resolve_todo_parallel
  ↓
扫描待办: todos/*-ready-*.md
  ↓
为每个待办派遣子命令:
  /workflows:work todos/001-ready-p1-*.md &
  /workflows:work todos/002-ready-p2-*.md &
  /workflows:work todos/003-ready-p2-*.md &
  ↓
并行执行,等待全部完成
  ↓
汇总结果
```

### 8.4 组件间通信协议

**通过文件通信**:

```
命令 A 输出:
  docs/plans/2026-01-31-feat-example-plan.md

命令 B 读取:
  1. 扫描 docs/plans/
  2. 按日期排序,找最新
  3. 读取计划内容
  4. 提取关键信息

通信协议:
  • 统一文件位置
  • 统一文件格式
  • 统一命名约定
```

**通过上下文通信**:

```
命令 A 设置上下文:
  "Design approved. Key decisions: ..."

命令 B 检测上下文:
  1. 检查对话历史
  2. 识别设计批准信号
  3. 自动触发
```

**通过 YAML frontmatter 通信**:

```
文件: todos/001-pending-p1-issue.md
---
status: pending
priority: p1
issue_id: 123
dependencies: [002, 003]
---

命令读取:
1. 解析 YAML frontmatter
2. 检查依赖关系
3. 决定执行顺序
```

### 8.5 错误处理与降级

**代理失败处理**:

```
场景: 代理执行超时或失败

处理:
1. 捕获错误
2. 记录错误日志
3. 降级策略:
   ├─ 重试 (3 次最大)
   ├─ 跳过非关键代理
   └─ 通知用户失败原因
4. 继续工作流 (如可能)
```

**技能缺失处理**:

```
场景: 引用的技能不存在

处理:
1. 检测技能缺失
2. 警告用户
3. 降级策略:
   ├─ 使用内联指导 (如有)
   ├─ 使用通用最佳实践
   └─ 建议用户创建技能
4. 继续执行 (不阻塞)
```

---

## 9. 扩展组件系统

### 9.1 创建个人代理

**步骤 1: 创建代理文件**

```bash
mkdir -p ~/.claude/agents
cat > ~/.claude/agents/my-custom-reviewer.md <<'EOF'
---
name: my-custom-reviewer
description: "Use this agent when reviewing code for [your specific criteria]"
---

# My Custom Reviewer

You are a specialized code reviewer focusing on [your criteria].

## Review Principles

1. Principle 1
   • Detail 1
   • Detail 2

2. Principle 2
   • Detail 1

## Review Process

1. Step 1
2. Step 2

## Output Format

Provide a structured review with:
- Issues found
- Severity levels
- Recommendations
EOF
```

**步骤 2: 测试代理**

```bash
# 测试调用
claude agent my-custom-reviewer "审查此代码: ..."

# 在命令中使用
Task my-custom-reviewer(代码内容)
```

**步骤 3: 版本控制 (可选)**

```bash
# 添加到 dotfiles 仓库
cd ~/dotfiles
git add .claude/agents/my-custom-reviewer.md
git commit -m "Add custom code reviewer agent"
git push
```

### 9.2 创建项目命令

**步骤 1: 创建命令文件**

```bash
mkdir -p .opencode/commands
cat > .opencode/commands/deploy.md <<'EOF'
---
name: deploy
description: Deploy this project to production
argument-hint: "[environment: staging|production]"
---

# Deploy Command

Deploy the application to the specified environment.

## Arguments

- `environment`: staging or production (default: staging)

## Execution Flow

### Phase 1: Pre-deployment Checks

1. Verify git status
   ```bash
   git status --porcelain
   # Should be empty
   ```

2. Run tests
   ```bash
   npm test
   ```

3. Build application
   ```bash
   npm run build
   ```

### Phase 2: Deployment

1. For staging:
   ```bash
   ./scripts/deploy-staging.sh
   ```

2. For production:
   ```bash
   ./scripts/deploy-production.sh
   ```

### Phase 3: Post-deployment Verification

1. Health check
2. Smoke tests
3. Monitor logs

## Output

- Deployment status
- Application URL
- Logs location
EOF
```

**步骤 2: 测试命令**

```bash
# 测试调用
/deploy staging

# 或完整调用
claude /deploy production
```

**步骤 3: 版本控制**

```bash
git add .opencode/commands/deploy.md
git commit -m "Add deployment command"
git push
```

### 9.3 创建技能

**使用 skill-creator 技能**:

```bash
# 调用技能创作技能
skill: skill-creator

# 或使用命令
/create-agent-skill
```

**手动创建**:

```bash
mkdir -p ~/.claude/skills/my-workflow
cat > ~/.claude/skills/my-workflow/SKILL.md <<'EOF'
---
name: my-workflow
description: This skill should be used when executing [your specific workflow]
---

# My Workflow

## When to Use This Skill

✅ Use when:
- Scenario 1
- Scenario 2

❌ Skip when:
- Scenario 3

## Core Process

### Phase 1: Preparation

[Steps]

### Phase 2: Execution

[Steps]

### Phase 3: Verification

[Steps]

## Best Practices

✅ Do:
- Practice 1
- Practice 2

❌ Don't:
- Anti-pattern 1

## Supporting Resources

- [Reference](./references/guide.md)
EOF
```

**添加参考文档**:

```bash
mkdir -p ~/.claude/skills/my-workflow/references
cat > ~/.claude/skills/my-workflow/references/guide.md <<'EOF'
# Detailed Guide

[Content]
EOF
```

**验证技能**:

```bash
# 使用 heal-skill 命令验证
/heal-skill my-workflow

# 检查输出确认无错误
```

### 9.4 覆盖官方组件

**覆盖代理**:

```bash
# 1. 复制官方代理
cp plugins/compound-engineering/agents/review/kieran-rails-reviewer.md \
   ~/.claude/agents/kieran-rails-reviewer.md

# 2. 修改副本
vim ~/.claude/agents/kieran-rails-reviewer.md

# 修改示例:
# - 调整审查原则
# - 添加额外检查
# - 修改输出格式

# 3. 验证覆盖生效
claude agent kieran-rails-reviewer "test"
```

**覆盖技能**:

```bash
# 1. 复制官方技能
cp -r plugins/compound-engineering/skills/brainstorming \
      .opencode/skills/brainstorming

# 2. 修改项目副本
vim .opencode/skills/brainstorming/SKILL.md

# 修改示例:
# - 调整问题技巧
# - 添加项目特定指导
# - 修改文档模板

# 3. 仅此项目使用自定义版本
# (其他项目继续使用官方版本)
```

### 9.5 组件模板

**代理模板**:

```markdown
---
name: [agent-name]
description: "Use this agent when [trigger conditions]...

Examples:
- <example>
  Context: [context]
  user: \"[user input]\"
  assistant: \"[assistant response]\"
  <commentary>
  [Why this agent should be used]
  </commentary>
  </example>"
---

# [Agent Name]

You are [role description].

## Core Principles

1. [Principle 1]
   • [Detail]

## [Analysis/Review] Process

1. [Step 1]
2. [Step 2]

## Output Format

[Expected output structure]
```

**命令模板**:

```markdown
---
name: [command-name]
description: [Command description]
argument-hint: "[argument description]"
---

# [Command Title]

[Brief description]

## Execution Flow

### Phase 1: [Phase Name]

[Steps]

### Phase 2: [Phase Name]

[Steps]

## Output

[What is produced]
```

**技能模板**:

```markdown
---
name: [skill-name]
description: This skill should be used when [trigger conditions]
---

# [Skill Title]

## When to Use This Skill

✅ Use when:
- [Scenario 1]

❌ Skip when:
- [Scenario 2]

## Core Process

### Phase 1: [Phase Name]

[Steps]

## Best Practices

✅ Do:
- [Practice 1]

❌ Don't:
- [Anti-pattern 1]

## Supporting Resources

- [resource.md](./references/resource.md)
```

---

## 10. 组件使用最佳实践

### 10.1 选择合适的组件类型

**决策树**:

```
需求: 我想添加新功能到系统

问题 1: 这是用户触发的还是自动的?
  ├─ 用户触发 → 创建 Command
  └─ 自动触发 → 考虑 Agent 或 Skill

问题 2: 这是执行任务还是提供知识?
  ├─ 执行任务 → Agent
  └─ 提供知识 → Skill

问题 3: 这是通用的还是项目特定的?
  ├─ 通用 → 个人组件 (~/.claude/)
  ├─ 项目特定 → 项目组件 (.opencode/)
  └─ 想贡献给社区 → 提交 PR 到插件

问题 4: 这需要专业化分析还是流程指导?
  ├─ 专业化分析 → Agent
  └─ 流程指导 → Skill
```

**组件类型对比**:

| 特征 | Agent | Command | Skill |
|------|-------|---------|-------|
| **触发方式** | 被调用 (Task) | 用户输入 (/cmd) | 被引用 (skill:) |
| **主要内容** | 角色+原则+流程 | 执行阶段+调度 | 知识+最佳实践 |
| **输出** | 分析结果 | 工件+文件 | 指导 (无直接输出) |
| **专业化** | 高 (特定领域) | 中 (特定任务) | 低 (通用流程) |
| **复用性** | 中 (跨命令) | 低 (独立使用) | 高 (跨组件) |
| **适用场景** | 代码审查、研究 | 工作流编排 | 流程标准化 |

### 10.2 命名约定

**Agent 命名**:

```
✅ 好名字:
  • kieran-rails-reviewer (角色-专业-功能)
  • security-sentinel (功能-隐喻)
  • repo-research-analyst (对象-功能-角色)

❌ 坏名字:
  • reviewer (太泛)
  • rails (不清楚功能)
  • agent-1 (无意义)
```

**Command 命名**:

```
✅ 好名字:
  • workflows:brainstorm (命名空间:功能)
  • deepen-plan (动词-对象)
  • triage (动词/专业术语)

❌ 坏名字:
  • plan (与内置冲突)
  • do-stuff (不具体)
  • cmd1 (无意义)
```

**Skill 命名**:

```
✅ 好名字:
  • agent-native-architecture (技术-形容词-领域)
  • brainstorming (动名词/流程)
  • git-worktree (工具-功能)

❌ 坏名字:
  • architecture (太泛)
  • skill-1 (无意义)
  • my-skill (不描述性)
```

### 10.3 文档撰写

**清晰的描述**:

```
✅ 好描述:
"Use this agent when reviewing Rails code changes with an
extremely high quality bar. This agent should be invoked
after implementing features, modifying existing code, or
creating new Rails components."

❌ 坏描述:
"Reviews code"
"Use for Rails"
```

**具体的触发条件**:

```
✅ 好触发条件:
"✅ Use when:
- Implementing new features
- Modifying existing code
- Creating new components
- PR review time

❌ Skip when:
- Simple bug fixes
- Documentation updates"

❌ 坏触发条件:
"Use when needed"
"For code review"
```

**可操作的步骤**:

```
✅ 好步骤:
"1. Read the plan document completely
2. Ask clarifying questions if anything is unclear
3. Create a todo list using TodoWrite
4. Execute tasks one by one"

❌ 坏步骤:
"1. Understand the plan
2. Do the work
3. Finish"
```

### 10.4 测试组件

**Agent 测试**:

```bash
# 1. 单独测试
claude agent my-agent "test input"

# 2. 在命令中测试
Task my-agent(test_input)

# 3. 验证输出格式
# - 检查结构
# - 检查完整性
# - 检查准确性

# 4. 边缘案例测试
# - 空输入
# - 大输入
# - 特殊字符
```

**Command 测试**:

```bash
# 1. 测试基本执行
/my-command

# 2. 测试带参数
/my-command arg1 arg2

# 3. 测试错误处理
/my-command invalid-arg

# 4. 验证输出
# - 检查文件创建
# - 检查文件内容
# - 检查提交信息
```

**Skill 测试**:

```bash
# 1. 验证格式
/heal-skill my-skill

# 2. 测试在命令中引用
# (在命令文档中添加 skill: my-skill)

# 3. 验证参考链接
# - 检查所有链接有效
# - 检查参考文件存在

# 4. 验证描述格式
grep '^description:' skills/my-skill/SKILL.md | grep 'This skill'
```

### 10.5 维护组件

**定期审查**:

```
每季度审查:
  ├─ 组件是否仍相关?
  ├─ 是否有新的最佳实践?
  ├─ 是否需要更新示例?
  ├─ 是否需要添加新参考?
  └─ 是否有用户反馈?

更新触发:
  ├─ 框架/库重大更新
  ├─ 项目约定变更
  ├─ 发现新反模式
  └─ 用户报告问题
```

**版本控制**:

```
个人组件:
  • 可选版本控制
  • 使用 dotfiles 仓库
  • 注意 .gitignore 敏感信息

项目组件:
  • 强烈推荐版本控制
  • 提交到项目仓库
  • 团队协作共享

变更记录:
  • 使用有意义的提交消息
  • 记录重大变更
  • 链接相关 issue/PR
```

**弃用组件**:

```
步骤:
1. 添加弃用警告到描述
   "⚠️ DEPRECATED: Use new-component instead"

2. 更新文档指向新组件
3. 保留旧组件一段时间 (如 3 个月)
4. 完全移除旧组件
5. 更新所有引用
```

---

## 11. 总结

Compound Engineering Plugin 的组件系统是一个 **模块化、可扩展、优先级分明** 的 AI 驱动开发工具架构。

### 11.1 核心特性

**三层架构**:
```
命令层 (20) → 用户交互入口
代理层 (27) → 专业化执行引擎
技能层 (14) → 可复用知识库
```

**三级优先级**:
```
个人组件 > 项目组件 > 官方组件
(可定制 > 可共享 > 可扩展)
```

**三种扩展**:
```
覆盖官方组件
创建项目组件
贡献个人组件
```

### 11.2 设计哲学

1. **专业化** - 27 个专业代理,每个专注一个领域
2. **模块化** - 组件独立,可组合,可替换
3. **可发现** - 统一命名,清晰文档,自动发现
4. **可扩展** - 三级优先级,遮蔽机制,灵活覆盖
5. **知识化** - 技能系统捕获流程知识,可复用

### 11.3 最佳实践总结

**选择组件类型**:
- 用户触发 → Command
- 专业分析 → Agent
- 流程知识 → Skill

**命名约定**:
- 描述性、具体、一致
- 避免冲突、缩写、泛化

**文档撰写**:
- 清晰描述、具体触发、可操作步骤
- 包含示例、反模式、参考资源

**测试验证**:
- 单独测试、集成测试、边缘案例
- 验证格式、输出、链接

**维护更新**:
- 定期审查、版本控制、变更记录
- 响应反馈、适应变化、及时弃用

### 11.4 关键收获

Compound Engineering Plugin 的组件系统不仅提供了 **61 个开箱即用的组件**,更重要的是提供了一个 **可扩展的框架**:

- ✅ 个人可以定制组件满足个人偏好
- ✅ 团队可以创建组件标准化流程
- ✅ 社区可以贡献组件丰富生态

**这不是静态工具集,而是活的知识系统。**

每个组件都是:
- 可学习的 (清晰文档)
- 可复用的 (跨项目)
- 可改进的 (持续迭代)
- 可分享的 (团队/社区)

**Compound Engineering Plugin 的组件系统让 AI 代理拥有 "工具箱思维" - 合适的工具,合适的时机,合适的方式。**

---

**文档完成**。这是一份 2380+ 行的综合性文档,涵盖了组件系统的所有方面,遵循了参考文档的专业中文风格。
