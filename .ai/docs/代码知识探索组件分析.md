# Compound-Engineering 插件：代码知识探索组件分析

> 本文档分析了 compound-engineering 插件中与探索存量代码知识相关的组件

## 📊 组件统计总览

| 组件类型 | 数量 | 说明 |
|---------|------|------|
| **AI 代理 (Agents)** | 8 个 | 研究类(5) + 审查类(2) + 工作流类(1) |
| **技能 (Skills)** | 4 个 | 知识管理、文档化、头脑风暴、工作流 |
| **命令 (Commands)** | 3 个 | 工作流命令 |
| **MCP 服务器** | 1 个 | 框架文档查询服务 |

---

## 🤖 研究类代理 (Research Agents) - 5个

这些代理专门用于深入理解和挖掘代码库中的知识。

### 1. repo-research-analyst - 仓库结构研究分析师

**文件位置**: `plugins/compound-engineering/agents/research/repo-research-analyst.md`

**核心职责**:
- 分析仓库的架构和组织结构
- 检查关键文档（ARCHITECTURE.md, README.md, CONTRIBUTING.md, CLAUDE.md）
- 搜索实现模式和命名约定
- 识别 GitHub issues 中的模式和标签使用规范
- 发现项目模板（issue templates, PR templates）

**使用场景**:
- 理解新仓库的结构和约定
- 准备创建 GitHub issue 前了解项目规范
- 实现新功能前查找现有模式

**输出格式**:
```markdown
## Repository Research Summary
### Architecture & Structure
### Issue Conventions
### Documentation Insights
### Templates Found
### Implementation Patterns
### Recommendations
```

**搜索策略**:
- 使用 Grep 工具进行文本/代码模式搜索
- 使用 Glob 工具按模式发现文件
- 使用 Read 工具读取文件内容
- 支持 AST-based 代码模式搜索（ast-grep）

---

### 2. git-history-analyzer - Git 历史分析器

**文件位置**: `plugins/compound-engineering/agents/research/git-history-analyzer.md`

**核心职责**:
- 追踪文件演变历史（`git log --follow --oneline -20`）
- 分析代码起源（`git blame -w -C -C -C`）
- 识别代码模式何时引入或移除（`git log -S"pattern"`）
- 映射贡献者和他们的专业领域（`git shortlog -sn`）
- 提取历史问题和解决方案的模式（`git log --grep`）

**使用场景**:
- 理解最近修改文件的历史背景
- 了解为什么存在某些代码模式
- 追踪代码重构和重命名
- 识别重复出现的问题主题

**输出格式**:
- **Timeline of File Evolution**: 主要变更的时间线
- **Key Contributors and Domains**: 主要贡献者及其专业领域
- **Historical Issues and Fixes**: 历史问题和解决方案的模式
- **Pattern of Changes**: 开发中的重复主题

**分析方法**:
- 从文件历史的广泛视图开始
- 在代码变更和提交消息中寻找模式
- 识别代码库中的转折点或重大重构
- 将贡献者与他们的专业领域关联

---

### 3. learnings-researcher - 学习知识研究员

**文件位置**: `plugins/compound-engineering/agents/research/learnings-researcher.md`

**核心职责**:
- 搜索 `docs/solutions/` 中的机构知识
- 通过 YAML frontmatter 元数据高效过滤已记录的解决方案
- 在实施新功能前查找相关的过往经验
- 防止重复犯错，应用已验证的模式

**高效搜索策略（关键！）**:

#### 步骤 1: 从功能描述中提取关键词
- 模块名称（如 "BriefSystem", "EmailProcessing", "payments"）
- 技术术语（如 "N+1", "caching", "authentication"）
- 问题指标（如 "slow", "error", "timeout", "memory"）
- 组件类型（如 "model", "controller", "job", "api"）

#### 步骤 2: 基于类别缩小范围（可选但推荐）

| 功能类型 | 搜索目录 |
|---------|---------|
| 性能优化 | `docs/solutions/performance-issues/` |
| 数据库变更 | `docs/solutions/database-issues/` |
| Bug 修复 | `docs/solutions/runtime-errors/`, `docs/solutions/logic-errors/` |
| 安全 | `docs/solutions/security-issues/` |
| UI 工作 | `docs/solutions/ui-bugs/` |
| 集成 | `docs/solutions/integration-issues/` |

#### 步骤 3: Grep 预过滤（关键效率提升！）

**并行运行多个 Grep 调用**:
```bash
Grep: pattern="title:.*email" path=docs/solutions/ output_mode=files_with_matches -i=true
Grep: pattern="tags:.*(email|mail|smtp)" path=docs/solutions/ output_mode=files_with_matches -i=true
Grep: pattern="module:.*(Brief|Email)" path=docs/solutions/ output_mode=files_with_matches -i=true
```

**为什么有效**: Grep 扫描文件内容而不读入上下文，只返回匹配的文件名，从 200 个文件减少到 5-20 个候选文件。

#### 步骤 3b: 始终检查关键模式

**无论 Grep 结果如何**，始终读取:
```bash
Read: docs/solutions/patterns/critical-patterns.md
```

这个文件包含适用于所有工作的必知模式——已提升为必读的高严重性问题。

#### 步骤 4: 仅读取候选文件的 Frontmatter

```bash
# 仅读取 frontmatter（限制前 30 行）
Read: [file_path] with limit:30
```

**YAML frontmatter 字段**:
```yaml
module: Brief System
problem_type: performance_issue
component: rails_model
symptoms: ["N+1 query", "slow response"]
root_cause: missing_include
tags: [n-plus-one, eager-loading, performance]
severity: high
```

#### 步骤 5: 评分和排序相关性

**强匹配（优先）**:
- `module` 匹配功能的目标模块
- `tags` 包含功能描述中的关键词
- `symptoms` 描述类似的可观察行为
- `component` 匹配正在接触的技术区域

**中等匹配（包括）**:
- `problem_type` 相关
- `root_cause` 建议可能适用的模式
- 提到相关模块或组件

**弱匹配（跳过）**:
- 无重叠的标签、症状或模块
- 不相关的问题类型

#### 步骤 6: 完整读取相关文件

仅对通过过滤的文件（强或中等匹配），读取完整文档以提取：
- 完整的问题描述
- 实施的解决方案
- 预防指导
- 代码示例

**输出格式**:
```markdown
## Institutional Learnings Search Results

### Search Context
- **Feature/Task**: [正在实施的内容描述]
- **Keywords Used**: [搜索的标签、模块、症状]
- **Files Scanned**: [X 总文件数]
- **Relevant Matches**: [Y 文件]

### Critical Patterns (Always Check)
[来自 critical-patterns.md 的任何匹配模式]

### Relevant Learnings
#### 1. [标题]
- **File**: [路径]
- **Module**: [模块]
- **Relevance**: [为什么这对当前任务很重要]
- **Key Insight**: [需要应用的关键点或模式]

### Recommendations
- [基于学习的具体行动]
- [要遵循的模式]
- [要避免的陷阱]
```

**使用的模型**: `haiku` (快速高效)

---

### 4. best-practices-researcher - 最佳实践研究员

**文件位置**: `plugins/compound-engineering/agents/research/best-practices-researcher.md`

**核心职责**:
- 收集外部最佳实践
- 研究行业标准和模式
- 为特定技术栈查找推荐方法

---

### 5. framework-docs-researcher - 框架文档研究员

**文件位置**: `plugins/compound-engineering/agents/research/framework-docs-researcher.md`

**核心职责**:
- 研究框架官方文档
- 查找使用指南和 API 参考
- 理解框架推荐的模式

**注意**: 当前年份是 2026，搜索最新文档时使用此年份。

---

## 🔎 审查类代理 (Review Agents) - 相关的2个

### 6. pattern-recognition-specialist - 模式识别专家

**文件位置**: `plugins/compound-engineering/agents/review/pattern-recognition-specialist.md`

**核心职责**:
1. **设计模式检测**: 搜索并识别常见设计模式（Factory, Singleton, Observer, Strategy 等）
2. **反模式识别**: 系统扫描代码异味和反模式，包括：
   - TODO/FIXME/HACK 注释（技术债务指标）
   - God objects/类承担过多职责
   - 循环依赖
   - 类之间不适当的亲密关系
   - Feature envy 和其他耦合问题

3. **命名约定分析**: 评估命名一致性：
   - 变量、方法和函数
   - 类和模块
   - 文件和目录
   - 常量和配置值

4. **代码重复检测**: 使用工具如 jscpd 识别重复的代码块

5. **架构边界审查**: 分析层违规和架构边界：
   - 检查关注点的适当分离
   - 识别违反架构原则的跨层依赖
   - 确保模块尊重其预期边界

**工作流程**:
1. 使用 Grep 工具进行广泛的模式搜索（或使用 `ast-grep` 进行结构化 AST 匹配）
2. 编译已识别模式及其位置的综合列表
3. 搜索常见的反模式指标（TODO, FIXME, HACK, XXX）
4. 通过抽样代表性文件分析命名约定
5. 运行带适当参数的重复检测工具
6. 审查边界违规的架构结构

**输出格式**:
- **Pattern Usage Report**: 发现的设计模式列表、位置和实现质量
- **Anti-Pattern Locations**: 包含反模式的具体文件和行号，带严重性评估
- **Naming Consistency Analysis**: 命名约定遵守统计，带具体不一致示例
- **Code Duplication Metrics**: 量化的重复数据，带重构建议

**使用场景**:
- 分析代码库中的模式和潜在问题
- 确保新功能遵循现有模式
- 代码审查和质量检查

---

### 7. architecture-strategist - 架构策略师

**文件位置**: `plugins/compound-engineering/agents/review/architecture-strategist.md`

**核心职责**:
- 分析架构决策
- 评估设计选择
- 识别架构模式和反模式
- 提供架构改进建议

---

## 🔧 工作流代理 (Workflow Agents) - 相关的1个

### 8. spec-flow-analyzer - 规格流程分析器

**文件位置**: `plugins/compound-engineering/agents/workflow/spec-flow-analyzer.md`

**核心职责**:
- 分析规格文档、计划或功能描述中的用户流程
- 识别所有可能的用户旅程和排列组合
- 发现规格中的空白、歧义和缺失元素
- 提出需要澄清的问题

**使用时机**:
- 用户提供功能规格、计划或需求文档
- 用户要求审查或验证设计或实施计划
- 用户描述需要流程分析的新功能或集成
- 初始规划会话后验证完整性
- 复杂用户面向功能的实施开始前

**分析流程**:

#### Phase 1: 深度流程分析
- 映射每个不同的用户旅程从开始到结束
- 识别所有决策点、分支和条件路径
- 考虑不同的用户类型、角色和权限级别
- 思考快乐路径、错误状态和边缘情况
- 检查状态转换和系统响应
- 考虑与现有功能的集成点

#### Phase 2: 排列组合发现
对于每个功能，系统考虑：
- 首次用户 vs. 返回用户场景
- 功能的不同入口点
- 各种设备类型和上下文（移动、桌面、平板）
- 网络条件（离线、慢连接、完美连接）
- 并发用户操作和竞态条件
- 部分完成和恢复场景
- 错误恢复和重试流程

#### Phase 3: 差距识别
识别并记录：
- 缺失的错误处理规格
- 不清晰的状态管理
- 模糊的用户反馈机制
- 未指定的验证规则
- 缺失的可访问性考虑
- 不清晰的数据持久化要求
- 未定义的超时或速率限制行为
- 缺失的安全考虑
- 不清晰的集成合同

#### Phase 4: 问题制定
对于每个差距或歧义，制定：
- 具体的、可操作的问题
- 为什么这很重要的上下文
- 如果未指定的潜在影响
- 说明歧义的示例

**输出格式**:
```markdown
### User Flow Overview
[所有已识别用户流程的清晰、结构化分解]

### Flow Permutations Matrix
[显示基于以下维度的每个流程的不同变体的矩阵或表格：
- 用户状态（已认证、访客、管理员等）
- 上下文（首次、返回、错误恢复）
- 设备/平台
- 任何其他相关维度]

### Missing Elements & Gaps
[按类别组织，列出所有已识别的差距]

### Critical Questions Requiring Clarification
[按优先级排序的具体问题编号列表：
1. Critical（阻止实施或产生安全/数据风险）
2. Important（显著影响 UX 或可维护性）
3. Nice-to-have（提高清晰度但有合理默认值）]

### Recommended Next Steps
[解决差距和问题的具体行动]
```

**关键原则**:
- **彻底透彻** - 假设规格将完全按书面实施，所以每个差距都很重要
- **像用户一样思考** - 像实际使用功能一样走过流程
- **考虑不快乐的路径** - 错误、失败和边缘情况是大多数差距隐藏的地方
- **在问题中具体** - 避免"错误怎么办？"，而是"当 OAuth 提供商返回 429 速率限制错误时应该发生什么？"

---

## 📝 技能 (Skills) - 相关的4个

### 9. compound-docs - 复合文档技能

**文件位置**: `plugins/compound-engineering/skills/compound-docs/SKILL.md`

**核心目的**: 自动将已解决的问题文档化为可搜索的机构知识，使用基于类别的组织（枚举验证的问题类型）。

**组织结构**: 单文件架构 - 每个问题记录为其症状类别目录中的一个 markdown 文件（例如 `docs/solutions/performance-issues/n-plus-one-briefs.md`）。文件使用 YAML frontmatter 进行元数据和可搜索性。

**7 步流程**:

#### Step 1: 检测确认
**自动调用的短语**:
- "that worked"
- "it's fixed"
- "working now"
- "problem solved"
- "that did it"

**或手动**: `/doc-fix` 命令

**非琐碎问题才记录**:
- 需要多次调查尝试
- 花费时间的棘手调试
- 非显而易见的解决方案
- 未来会话会受益

**跳过文档的情况**:
- 简单的拼写错误
- 明显的语法错误
- 立即纠正的琐碎修复

#### Step 2: 收集上下文
从对话历史中提取：

**必需信息**:
- **Module name**: 哪个模块或组件有问题
- **Symptom**: 可观察的错误/行为（确切的错误消息）
- **Investigation attempts**: 什么不起作用以及为什么
- **Root cause**: 实际问题的技术解释
- **Solution**: 什么修复了它（代码/配置更改）
- **Prevention**: 如何在未来避免

**环境详细信息**:
- Rails 版本
- 阶段（0-6 或实施后）
- OS 版本
- 文件/行引用

**阻塞要求**: 如果缺少关键上下文（模块名称、确切错误、阶段或解决步骤），询问用户并等待响应后再继续。

#### Step 3: 检查现有文档（可选）
搜索 docs/solutions/ 中的类似问题：

```bash
# 按错误消息关键字搜索
grep -r "exact error phrase" docs/solutions/

# 按症状类别搜索
ls docs/solutions/[category]/
```

**如果找到类似问题**: 呈现决策选项并等待用户响应。

#### Step 4: 生成文件名
格式: `[sanitized-symptom]-[module]-[YYYYMMDD].md`

**清理规则**:
- 小写
- 用连字符替换空格
- 除连字符外删除特殊字符
- 截断到合理长度（< 80 字符）

**示例**:
- `missing-include-BriefSystem-20251110.md`
- `parameter-not-saving-state-EmailProcessing-20251110.md`

#### Step 5: 验证 YAML Schema（阻塞！）
**关键**: 所有文档都需要经过验证的 YAML frontmatter，带枚举验证。

加载 `schema.yaml` 并根据 [yaml-schema.md](./references/yaml-schema.md) 中定义的枚举值对问题进行分类。

**如果验证失败则阻止**: 不要进行到步骤 6（创建文档），直到 YAML frontmatter 通过 `schema.yaml` 中定义的所有验证规则。

#### Step 6: 创建文档
**从 problem_type 确定类别**: 使用 [yaml-schema.md](./references/yaml-schema.md) 中定义的类别映射（第 49-61 行）。

```bash
PROBLEM_TYPE="[from validated YAML]"
CATEGORY="[mapped from problem_type]"
FILENAME="[generated-filename].md"
DOC_PATH="docs/solutions/${CATEGORY}/${FILENAME}"

# 如果需要创建目录
mkdir -p "docs/solutions/${CATEGORY}"

# 使用 assets/resolution-template.md 中的模板编写文档
```

**创建文档**: 使用步骤 2 收集的上下文和步骤 5 验证的 YAML frontmatter 填充 `assets/resolution-template.md` 中的结构。

#### Step 7: 交叉引用和关键模式检测（可选）
如果在步骤 3 中找到类似问题：
- 向类似文档添加相关问题链接
- 更新新文档（已包含来自步骤 6 的交叉引用）

**如果这代表一个常见模式**（3+ 类似问题）:
```bash
# 添加到 docs/solutions/patterns/common-solutions.md
```

**关键模式检测（可选的主动建议）**:
如果此问题具有自动指标，表明可能是关键的：
- 严重性：YAML 中的 `critical`
- 影响多个模块或基础阶段（阶段 2 或 3）
- 非显而易见的解决方案

则在决策菜单（步骤 8）中添加注释：
```
💡 这可能值得添加到必读清单（选项 2）
```

但**永远不要自动提升**。用户通过决策菜单决定（选项 2）。

**关键模式添加模板**: 当用户选择选项 2（添加到必读清单）时，使用 `assets/critical-pattern-template.md` 中的模板来构建模式条目。

**文档成功后的决策菜单**:
```
✓ Solution documented

File created:
- docs/solutions/[category]/[filename].md

What's next?
1. Continue workflow (recommended)
2. Add to Required Reading - 提升为关键模式 (critical-patterns.md)
3. Link related issues - 连接到类似问题
4. Add to existing skill - 添加到学习技能（如 hotwire-native）
5. Create new skill - 提取到新学习技能
6. View documentation - 查看捕获的内容
7. Other
```

**选项说明**:

**Option 1: Continue workflow** - 返回调用技能/工作流，文档完成

**Option 2: Add to Required Reading** ⭐ 关键模式的主要路径
用户在以下情况选择：
- 系统在不同模块中多次犯此错误
- 解决方案非显而易见但每次都必须遵循
- 基础要求（Rails, Rails API, threading 等）

操作：
1. 从文档中提取模式
2. 格式化为 ❌ 错误 vs ✅ 正确，带代码示例
3. 添加到 `docs/solutions/patterns/critical-patterns.md`
4. 添加交叉引用回此文档
5. 确认："✓ Added to Required Reading. All subagents will see this pattern before code generation."

**Option 3: Link related issues** - 提示相关文档，搜索并添加交叉引用

**Option 4: Add to existing skill** - 将文档化的解决方案添加到现有学习技能
例如：对于 Hotwire Native Tailwind 变体解决方案：
- 添加到 `hotwire-native/references/resources.md` 的"项目特定资源"下
- 添加到 `hotwire-native/references/examples.md`，带解决方案文档链接

**Option 5: Create new skill** - 当解决方案代表新学习领域的开始时
操作：
1. 提示："新技能应该叫什么？（例如 stripe-billing, email-processing）"
2. 运行 `python3 .claude/skills/skill-creator/scripts/init_skill.py [skill-name]`
3. 创建初始引用文件，此解决方案作为第一个示例

**集成点**:
- 由 `/compound` 命令调用（主要接口）
- 在解决方案确认后在对话中手动调用
- 可通过检测确认短语如"that worked"、"it's fixed"等触发

**成功标准**:
- ✅ YAML frontmatter 已验证（所有必填字段，正确格式）
- ✅ 文件在 docs/solutions/[category]/[filename].md 中创建
- ✅ 枚举值与 schema.yaml 完全匹配
- ✅ 解决方案部分包含代码示例
- ✅ 如果找到相关问题，添加交叉引用
- ✅ 向用户呈现决策菜单并确认操作

**质量指南**:

**好的文档具有**:
- ✅ 确切的错误消息（从输出复制粘贴）
- ✅ 具体的文件:行引用
- ✅ 可观察的症状（你看到的，而不是解释）
- ✅ 记录失败的尝试（帮助避免错误路径）
- ✅ 技术解释（不仅是"什么"，还有"为什么"）
- ✅ 代码示例（如适用，包括之前/之后）
- ✅ 预防指导（如何早期发现）
- ✅ 交叉引用（相关问题）

**避免**:
- ❌ 模糊描述（"有问题"）
- ❌ 缺少技术细节（"修复了代码"）
- ❌ 无上下文（哪个版本？哪个文件？）
- ❌ 只是代码转储（解释为什么有效）
- ❌ 无预防指导
- ❌ 无交叉引用

---

### 10. file-todos - 基于文件的待办事项追踪

**文件位置**: `plugins/compound-engineering/skills/file-todos/SKILL.md`

**概述**: `todos/` 目录包含一个基于文件的跟踪系统，用于管理代码审查反馈、技术债务、功能请求和工作项。每个 todo 是一个带有 YAML frontmatter 和结构化部分的 markdown 文件。

**何时使用此技能**:
- 从发现或反馈创建新的 todos
- 管理 todo 生命周期（pending → ready → complete）
- 分类待批准的项目
- 检查或管理依赖关系
- 将 PR 评论或代码发现转换为跟踪的工作
- 在 todo 执行期间更新工作日志

**文件命名约定**:
```
{issue_id}-{status}-{priority}-{description}.md
```

**组件**:
- **issue_id**: 序列号（001, 002, 003...）- 永不重复使用
- **status**: `pending`（需要分类）、`ready`（已批准）、`complete`（完成）
- **priority**: `p1`（关键）、`p2`（重要）、`p3`（好有）
- **description**: kebab-case，简要描述

**示例**:
```
001-pending-p1-mailer-test.md
002-ready-p1-fix-n-plus-1.md
005-complete-p2-refactor-csv.md
```

**文件结构**:

每个 todo 是一个带有 YAML frontmatter 和结构化部分的 markdown 文件。创建新 todos 时使用 [todo-template.md](./assets/todo-template.md) 模板作为起点。

**必需部分**:
- **Problem Statement** - 什么被破坏、缺失或需要改进？
- **Findings** - 调查结果、根本原因、关键发现
- **Proposed Solutions** - 多个选项，带优缺点、工作量、风险
- **Recommended Action** - 明确计划（在分类期间填写）
- **Acceptance Criteria** - 可测试的检查列表项
- **Work Log** - 带日期、操作、学习的时间顺序记录

**可选部分**:
- **Technical Details** - 受影响的文件、相关组件、DB 更改
- **Resources** - 错误、测试、PR、文档链接
- **Notes** - 附加上下文或决策

**YAML frontmatter 字段**:
```yaml
---
status: ready              # pending | ready | complete
priority: p1              # p1 | p2 | p3
issue_id: "002"
tags: [rails, performance, database]
dependencies: ["001"]     # 此任务被阻止的问题 ID
---
```

**常见工作流**:

**创建新 Todo**:
1. 确定下一个问题 ID: `ls todos/ | grep -o '^[0-9]\+' | sort -n | tail -1`
2. 复制模板: `cp assets/todo-template.md todos/{NEXT_ID}-pending-{priority}-{description}.md`
3. 编辑并填写必需部分
4. 确定状态：`pending`（需要分类）或 `ready`（预先批准）
5. 添加相关标签以进行过滤

**何时创建 todo**:
- 需要超过 15-20 分钟的工作
- 需要研究、规划或考虑多种方法
- 依赖于其他工作
- 需要经理批准或优先级排序
- 更大功能或重构的一部分
- 需要文档化的技术债务

**何时立即行动**:
- 问题很琐碎（< 15 分钟）
- 现在有完整的上下文
- 不需要规划
- 用户明确要求立即行动
- 具有明显解决方案的简单错误修复

**分类待处理项目**:
1. 列出待处理项目: `ls todos/*-pending-*.md`
2. 对于每个 todo：阅读问题陈述和发现，审查建议的解决方案，做出决定
3. 更新已批准的 todos：
   - 重命名文件: `mv {file}-pending-{pri}-{desc}.md {file}-ready-{pri}-{desc}.md`
   - 更新 frontmatter: `status: pending` → `status: ready`
   - 填写"推荐操作"部分，提供明确计划

**使用斜杠命令**: `/triage` 用于交互式批准工作流

**管理依赖关系**:
```yaml
dependencies: ["002", "005"]  # 此 todo 被问题 002 和 005 阻止
dependencies: []               # 无阻止者 - 可以立即工作
```

**检查什么阻止了 todo**:
```bash
grep "^dependencies:" todos/003-*.md
```

**查找 todo 阻止了什么**:
```bash
grep -l 'dependencies:.*"002"' todos/*.md
```

**更新工作日志**:
在处理 todo 时，始终添加工作日志条目：
```markdown
### YYYY-MM-DD - Session Title

**By:** Claude Code / Developer Name

**Actions:**
- 具体更改（包括文件:行引用）
- 执行的命令
- 运行的测试
- 调查结果

**Learnings:**
- 什么有效/什么无效
- 发现的模式
- 未来工作的关键见解
```

**完成 Todo**:
1. 验证所有验收标准已勾选
2. 使用最终会话和结果更新工作日志
3. 重命名文件: `mv {file}-ready-{pri}-{desc}.md {file}-complete-{pri}-{desc}.md`
4. 更新 frontmatter: `status: ready` → `status: complete`
5. 检查未阻止的工作
6. 使用问题引用提交: `feat: resolve issue 002`

**与开发工作流集成**:

| 触发器 | 流程 | 工具 |
|---------|------|------|
| 代码审查 | `/workflows:review` → Findings → `/triage` → Todos | Review agent + skill |
| PR 评论 | `/resolve_pr_parallel` → Individual fixes → Todos | gh CLI + skill |
| 代码 TODOs | `/resolve_todo_parallel` → Fixes + Complex todos | Agent + skill |
| 规划 | Brainstorm → Create todo → Work → Complete | Skill |
| 反馈 | Discussion → Create todo → Triage → Work | Skill + slash |

**关键区别**:

**File-todos 系统（此技能）**:
- `todos/` 目录中的 Markdown 文件
- 开发/项目跟踪
- 带有 YAML frontmatter 的独立 markdown 文件
- 人类和代理使用

**Rails Todo 模型**:
- `app/models/todo.rb` 中的数据库模型
- 应用程序中面向用户的功能
- Active Record CRUD 操作
- 不同于此基于文件的系统

**TodoWrite 工具**:
- 代理会话期间的内存任务跟踪
- 单次对话的临时跟踪
- 不持久化到磁盘
- 不同于上述两个系统

---

### 11. brainstorming - 头脑风暴技能

**文件位置**: `plugins/compound-engineering/skills/brainstorming/SKILL.md`

**核心目的**: 在规划实施前通过协作对话探索需求和方法。

**何时使用**:
- 探索新功能想法或问题
- 在开始实施前澄清需求
- 理解现有模式并提出多个方法
- 记录设计决策

**与 /workflows:plan 的关系**:
- Brainstorming 回答 **WHAT** 要构建
- Planning 回答 **HOW** 构建

**输出**: `docs/brainstorms/YYYY-MM-DD-<topic>-brainstorm.md`

---

### 12. git-worktree - Git 工作树管理

**文件位置**: `plugins/compound-engineering/skills/git-worktree/SKILL.md`

**核心目的**: 管理 Git 工作树以实现并行开发。

**使用场景**:
- 同时在多个分支上工作
- 快速切换上下文而不丢失工作
- 并行测试不同的实现方法

---

## 💻 命令 (Commands) - 相关的3个

### 13. /workflows:brainstorm - 头脑风暴工作流

**文件位置**: `plugins/compound-engineering/commands/workflows/brainstorm.md`

**描述**: 在规划实施前通过协作对话探索需求和方法。

**参数提示**: `[feature idea or problem to explore]`

**执行流程**:

#### Phase 0: 评估需求清晰度
评估是否需要头脑风暴。

**清晰需求指标**:
- 提供了具体的验收标准
- 引用了现有模式
- 描述了确切的预期行为
- 限制的、定义明确的范围

**如果需求已经清晰**: 使用 **AskUserQuestion 工具**建议："您的需求似乎足够详细，可以直接进行规划。我应该运行 `/workflows:plan`，还是您想进一步探索这个想法？"

#### Phase 1: 理解想法

**1.1 仓库研究（轻量级）**:
运行快速仓库扫描以理解现有模式：
```
Task repo-research-analyst("Understand existing patterns related to: <feature_description>")
```

重点关注：类似功能、既定模式、CLAUDE.md 指导。

**1.2 协作对话**:
使用 **AskUserQuestion 工具**提出问题**一次一个**。

**指南**（详见 `brainstorming` 技能）:
- 当存在自然选项时优先选择多项选择
- 从广泛开始（目的、用户）然后缩小（约束、边缘情况）
- 明确验证假设
- 询问成功标准

**退出条件**: 继续直到想法清晰或用户说"继续"

#### Phase 2: 探索方法
基于研究和对话提出 **2-3 种具体方法**。

对于每种方法，提供：
- 简要描述（2-3 句话）
- 优缺点
- 最适合的场景

以您的推荐为主导并解释原因。应用 YAGNI——优先选择更简单的解决方案。

使用 **AskUserQuestion 工具**询问用户喜欢哪种方法。

#### Phase 3: 捕获设计
将头脑风暴文档写入 `docs/brainstorms/YYYY-MM-DD-<topic>-brainstorm.md`。

**文档结构**：见 `brainstorming` 技能的模板格式。关键部分：我们正在构建什么、为什么这种方法、关键决策、开放问题。

确保 `docs/brainstorms/` 目录在写入前存在。

#### Phase 4: 交接
使用 **AskUserQuestion 工具**呈现下一步：

**问题**: "Brainstorm captured. What would you like to do next?"

**选项**:
1. **Proceed to planning** - 运行 `/workflows:plan`（将自动检测此头脑风暴）
2. **Refine design further** - 继续探索
3. **Done for now** - 稍后返回

**输出摘要**:
```
Brainstorm complete!

Document: docs/brainstorms/YYYY-MM-DD-<topic>-brainstorm.md

Key decisions:
- [Decision 1]
- [Decision 2]

Next: Run `/workflows:plan` when ready to implement.
```

**重要指南**:
- **专注于 WHAT，而不是 HOW** - 实施细节属于计划
- **一次问一个问题** - 不要压倒
- **应用 YAGNI** - 优先选择更简单的方法
- **保持输出简洁** - 每个部分最多 200-300 字

**永远不要编码！** 只是探索和记录决策。

---

### 14. /deepen-plan - 深化计划

**文件位置**: `plugins/compound-engineering/commands/deepen-plan.md`

**描述**: 通过并行研究增强实施计划的深度。

**可能调用的代理**:
- learnings-researcher
- best-practices-researcher
- framework-docs-researcher
- repo-research-analyst

**使用场景**:
- 在初始计划创建后添加更多深度和上下文
- 查找相关的机构知识
- 研究最佳实践和框架文档

---

### 15. /triage - 分类

**文件位置**: `plugins/compound-engineering/commands/triage.md`

**描述**: 分类和优先级排序待处理的 todos 和问题。

**集成**: 与 file-todos 技能集成，提供交互式批准工作流。

**使用场景**:
- 审查待处理的 todos
- 批准工作项
- 设置优先级
- 管理依赖关系

---

## 🔌 MCP 服务器 - 1个

### 16. context7 - 框架文档查询服务

**配置位置**: `plugins/compound-engineering/mcp-servers/context7/`

**类型**: HTTP-based MCP 服务器

**URL**: `https://mcp.context7.com/mcp`

**提供的工具**:
1. **resolve-library-id** - 查找框架/包的库 ID
2. **get-library-docs** - 获取特定库的文档

**支持的框架**: 100+ 个框架，包括：
- **Ruby/Rails**: Rails, Sinatra, Hanami, Grape
- **JavaScript/TypeScript**: React, Next.js, Vue, Angular, Svelte, Node.js, Express
- **Python**: Django, Flask, FastAPI, SQLAlchemy
- **PHP**: Laravel, Symfony, CodeIgniter
- **其他**: Spring Boot, .NET Core, Go frameworks 等

**使用场景**:
- 快速查找框架文档而不离开 Claude Code
- 在实施前验证 API 使用方法
- 查找框架推荐的模式和最佳实践
- 在 framework-docs-researcher 代理中使用

**工作流集成**:
```
用户: "我需要使用 Rails Active Record associations"
↓
claude: 调用 context7 MCP 服务器
↓
resolve-library-id("rails") → library_id
↓
get-library-docs(library_id, topic="active record associations")
↓
返回官方 Rails 文档内容
```

---

## 💡 推荐工作流

### 🆕 探索新代码库

```bash
# Step 1: 理解仓库结构和约定
claude agent repo-research-analyst "分析这个项目的结构、文档和模式"

# Step 2: 分析核心文件的演变历史
claude agent git-history-analyzer "分析 [核心文件路径] 的历史演变"

# Step 3: 搜索已记录的机构知识
claude agent learnings-researcher "搜索与 [领域/模块] 相关的已记录知识"

# Step 4: 识别代码模式
claude agent pattern-recognition-specialist "分析代码库中的设计模式和反模式"
```

### 🚀 实施新功能前

```bash
# Step 1: 头脑风暴和探索
claude /workflows:brainstorm "新功能想法描述"
# 输出: docs/brainstorms/YYYY-MM-DD-topic-brainstorm.md

# Step 2: 搜索相关的过往解决方案
claude agent learnings-researcher "搜索与 [功能模块] 相关的解决方案"
# 检查: docs/solutions/patterns/critical-patterns.md（始终检查！）

# Step 3: 研究框架最佳实践
claude agent framework-docs-researcher "研究 [框架] 关于 [功能] 的文档"
# 或直接使用 context7 MCP 服务器

# Step 4: 创建实施计划
claude /workflows:plan
# 可选：使用 /deepen-plan 添加更多研究深度

# Step 5: 分析规格的用户流程
claude agent spec-flow-analyzer "分析这个功能规格"
```

### 📚 记录已解决的问题

```bash
# 方式 1: 自动检测（在解决问题后）
用户: "太好了，问题解决了！"
# compound-docs 技能自动激活

# 方式 2: 手动触发
claude /compound
# 或在对话中调用 compound-docs 技能

# 输出: docs/solutions/[category]/[filename].md
# 决策菜单:
# 1. Continue workflow
# 2. Add to Required Reading (critical-patterns.md)
# 3. Link related issues
# 4. Add to existing skill
# 5. Create new skill
# 6. View documentation
```

### 🔍 分析已有代码模式

```bash
# 识别设计模式和反模式
claude agent pattern-recognition-specialist "检查代码库中的模式和质量问题"

# 输出:
# - Pattern Usage Report（设计模式使用报告）
# - Anti-Pattern Locations（反模式位置）
# - Naming Consistency Analysis（命名一致性分析）
# - Code Duplication Metrics（代码重复度量）
```

### 📋 管理工作项和技术债务

```bash
# 列出待分类的项目
ls todos/*-pending-*.md

# 分类和批准
claude /triage

# 查找可以开始的工作（高优先级，无依赖）
grep -l 'dependencies: \[\]' todos/*-ready-p1-*.md

# 开始工作并更新日志
# 编辑 todos/XXX-ready-p1-description.md
# 添加工作日志条目

# 完成后标记为完成
mv todos/XXX-ready-p1-desc.md todos/XXX-complete-p1-desc.md
# 更新 frontmatter: status: complete
```

---

## 🎯 关键洞察

### 知识流动循环

```
代码实施 → 遇到问题 → 解决问题 → compound-docs 记录
                                            ↓
                        learnings-researcher ← docs/solutions/
                                            ↓
                              影响下一次实施决策
```

### 效率优化策略

1. **learnings-researcher 的 Grep-First 策略**:
   - 不要一次读取所有文件的 frontmatter
   - 先用 Grep 并行过滤（从 200 个文件减少到 5-20 个候选）
   - 只读取候选文件的 frontmatter（前 30 行）
   - 只完整读取真正相关的文件

2. **始终检查 critical-patterns.md**:
   - 无论 Grep 结果如何，始终读取此文件
   - 包含跨所有工作适用的必知模式
   - 高严重性问题已提升为必读

3. **使用并行工具调用**:
   - 在单个消息中进行多个 Grep 调用
   - 在单个消息中读取多个文件
   - 最大化效率

### 文档化决策树

```
问题已解决
    ↓
非琐碎？（>15分钟，非显而易见）
    ↓ 是
compound-docs 激活
    ↓
记录到 docs/solutions/[category]/
    ↓
决策菜单:
    ├─ Continue workflow（返回）
    ├─ Add to Required Reading（提升为 critical-patterns.md）
    ├─ Link related issues（交叉引用）
    ├─ Add to existing skill（添加到学习技能）
    └─ Create new skill（创建新技能）
```

### 集成点

```
/workflows:brainstorm
    ├─ 调用: repo-research-analyst（理解现有模式）
    ├─ 输出: docs/brainstorms/YYYY-MM-DD-topic.md
    └─ 交接: /workflows:plan

/workflows:plan
    ├─ 自动检测: docs/brainstorms/（如果存在）
    ├─ 可选调用: /deepen-plan
    │       ├─ learnings-researcher
    │       ├─ best-practices-researcher
    │       └─ framework-docs-researcher
    └─ 输出: 实施计划

/workflows:review
    ├─ 发现问题
    ├─ 输出: 审查反馈
    └─ 可选: /triage → file-todos

解决问题后
    ├─ compound-docs 激活
    ├─ 输出: docs/solutions/[category]/[file].md
    └─ 选项: 提升为 critical-patterns.md 或添加到技能
```

---

## 📖 YAML Schema 参考

### problem_type 枚举值
- `build_error` - 构建错误
- `test_failure` - 测试失败
- `runtime_error` - 运行时错误
- `performance_issue` - 性能问题
- `database_issue` - 数据库问题
- `security_issue` - 安全问题
- `ui_bug` - UI bug
- `integration_issue` - 集成问题
- `logic_error` - 逻辑错误
- `developer_experience` - 开发者体验
- `workflow_issue` - 工作流问题
- `best_practice` - 最佳实践
- `documentation_gap` - 文档缺口

### component 枚举值
- `rails_model`, `rails_controller`, `rails_view`, `service_object`
- `background_job`, `database`, `frontend_stimulus`, `hotwire_turbo`
- `email_processing`, `brief_system`, `assistant`, `authentication`
- `payments`, `development_workflow`, `testing_framework`, `documentation`, `tooling`

### root_cause 枚举值
- `missing_association`, `missing_include`, `missing_index`, `wrong_api`
- `scope_issue`, `thread_violation`, `async_timing`, `memory_leak`
- `config_error`, `logic_error`, `test_isolation`, `missing_validation`
- `missing_permission`, `missing_workflow_step`, `inadequate_documentation`
- `missing_tooling`, `incomplete_setup`

### 类别目录（从 problem_type 映射）
- `docs/solutions/build-errors/`
- `docs/solutions/test-failures/`
- `docs/solutions/runtime-errors/`
- `docs/solutions/performance-issues/`
- `docs/solutions/database-issues/`
- `docs/solutions/security-issues/`
- `docs/solutions/ui-bugs/`
- `docs/solutions/integration-issues/`
- `docs/solutions/logic-errors/`
- `docs/solutions/developer-experience/`
- `docs/solutions/workflow-issues/`
- `docs/solutions/best-practices/`
- `docs/solutions/documentation-gaps/`

---

## 🔗 相关资源

- **完整插件文档**: `plugins/compound-engineering/README.md`
- **开发指南**: `plugins/compound-engineering/CLAUDE.md`
- **变更日志**: `plugins/compound-engineering/CHANGELOG.md`
- **Skills 定义**: `plugins/compound-engineering/skills/*/SKILL.md`
- **Agents 定义**: `plugins/compound-engineering/agents/*/**.md`

---

## 📝 总结

这 16 个组件共同构成了一个完整的**知识挖掘、积累和重用系统**：

1. **挖掘知识**: 研究类代理从代码、历史、文档中提取知识
2. **记录知识**: compound-docs 和 file-todos 将解决方案结构化存储
3. **检索知识**: learnings-researcher 高效搜索和过滤已记录的知识
4. **应用知识**: 在规划和实施前使用过往经验指导决策
5. **积累知识**: 关键模式提升为必读，技能捕获领域专业知识

**核心理念**: 每个工程单元的工作都应该使后续工作变得更容易——而不是更难（Compounding Engineering）。
